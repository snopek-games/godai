#
# For task evaluation via `godai-eval`
#

# Uses Anthropic API key with usage-based billing
ANTHROPIC_API_KEY=""

# Uses Claude subscription instead of API key (run `claude setup-token` to obtain token)
CLAUDE_CODE_OAUTH_TOKEN=""


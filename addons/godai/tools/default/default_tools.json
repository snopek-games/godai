{
	"$schema": "./default_tools.schema.json",
	"tools": {
		"get_current_project": {
			"title": "Get Current Project",
			"toolsets": ["project"],
			"description": "Gets information about the project that is currently opened in the Godot editor.",
			"annotations": {
				"readOnlyHint": true,
				"openWorldHint": false
			},
			"outputSchema": {
				"type": "object",
				"properties": {
					"project_path": {
						"type": "string",
						"description": "The absolute path to the current project"
					},
					"project_name": {
						"type": "string",
						"description": "The project's configured name"
					},
					"headless": {
						"type": "boolean",
						"description": "Whether the editor is running with a headless display driver"
					},
					"godot_version": {
						"type": "string",
						"description": "The version of Godot this editor is running, for example: `4.5.stable.official`, or `4.4.1.stable.mono.official` for a .NET build"
					},
					"error": {
						"type": "string",
						"description": "Why the call failed; present instead of the other properties when it did"
					}
				}
			},
			"doNotForward": true
		},
		"get_project_settings": {
			"title": "Get Project Settings",
			"toolsets": ["project"],
			"description": [
				"Gets settings from the project (the values stored in 'project.godot', e.g. 'application/config/name' or 'display/window/size/viewport_width').",
				"",
				"Pass 'names' to get specific settings by name (returned regardless of whether they match their default).",
				"Otherwise, all settings are returned, but only those whose value differs from the default; pass 'include_defaults' as true to also include settings left at their default value.",
				"",
				"Setting values are strings in Godot variant syntax."
			],
			"annotations": {
				"readOnlyHint": true,
				"openWorldHint": false
			},
			"inputSchema": {
				"type": "object",
				"properties": {
					"names": {
						"type": "array",
						"description": "Specific setting names to get. If omitted, all settings are returned.",
						"items": {
							"type": "string"
						}
					},
					"include_defaults": {
						"type": "boolean",
						"description": "When getting all settings, also include those left at their default value (default: false)"
					}
				}
			},
			"outputSchema": {
				"type": "object",
				"properties": {
					"settings": {
						"type": "object",
						"description": "A map of setting name to value, in Godot variant syntax",
						"additionalProperties": {
							"type": "string"
						}
					},
					"error": {
						"type": "string",
						"description": "Why the call failed; present instead of the other properties when it did"
					}
				}
			}
		},
		"set_project_settings": {
			"title": "Set Project Settings",
			"toolsets": ["project"],
			"description": [
				"Changes settings in the project and saves 'project.godot'.",
				"",
				"Pass 'settings': a map of setting name (e.g. 'display/window/size/viewport_width') to value. Values are strings in Godot variant syntax (e.g. `1280`, `true`, `2.0`); a value that isn't valid variant syntax is taken as a raw string.",
				"",
				"Settings that don't exist yet are created.",
				"",
				"Use get_project_settings to see the available setting names and their current values."
			],
			"annotations": {
				"readOnlyHint": false,
				"destructiveHint": true,
				"idempotentHint": true,
				"openWorldHint": false
			},
			"inputSchema": {
				"type": "object",
				"properties": {
					"settings": {
						"type": "object",
						"minProperties": 1,
						"description": "Settings to set (keys are setting names)",
						"additionalProperties": {
							"type": "string",
							"description": "The setting value, as a string in Godot variant syntax"
						}
					}
				},
				"required": ["settings"]
			},
			"outputSchema": {
				"type": "object",
				"properties": {
					"success": {
						"type": "boolean",
						"description": "True if the settings were changed and the project was saved"
					},
					"error": {
						"type": "string",
						"description": "Why the call failed; present instead of the other properties when it did"
					}
				}
			}
		},
		"get_current_scene": {
			"title": "Get Current Scene",
			"toolsets": ["scene"],
			"description": "Gets information about the scene that is currently opened in the Godot editor.",
			"annotations": {
				"readOnlyHint": true,
				"openWorldHint": false
			},
			"outputSchema": {
				"type": "object",
				"properties": {
					"scene_path": {
						"type": "string",
						"description": "The path to the scene file, or '[unsaved]' for a never-saved scene"
					},
					"root_node_type": {
						"type": "string",
						"description": "The class of the root Node"
					},
					"root_node_name": {
						"type": "string",
						"description": "The name of the root Node"
					},
					"error": {
						"type": "string",
						"description": "Why the call failed; present instead of the other properties when it did"
					}
				}
			}
		},
		"get_current_scene_tree": {
			"title": "Get Scene Tree",
			"toolsets": ["scene"],
			"description": "Gets the tree for the scene that is currently opened in the Godot editor.",
			"annotations": {
				"readOnlyHint": true,
				"openWorldHint": false
			},
			"outputSchema": {
				"type": "object",
				"description": "A node in the scene tree. Children share this same structure.",
				"properties": {
					"name": {
						"type": "string",
						"description": "The Node name"
					},
					"type": {
						"type": "string",
						"description": "The class of the Node"
					},
					"path": {
						"type": "string",
						"description": "The path of the node within the scene, relative to the scene root"
					},
					"script": {
						"type": "string",
						"description": "The path to the script file, if this node has a script attached; otherwise omitted"
					},
					"children": {
						"type": "array",
						"description": "The child nodes; omitted if the node has no children",
						"items": {
							"$ref": "#"
						}
					},
					"error": {
						"type": "string",
						"description": "Why the call failed; present instead of the other properties when it did"
					}
				}
			}
		},
		"create_scene": {
			"title": "Create Scene",
			"toolsets": ["scene"],
			"description": "Creates a new scene and opens it in the editor.",
			"inputSchema": {
				"type": "object",
				"properties": {
					"file_path": {
						"type": "string",
						"minLength": 1,
						"description": "Path to the .tscn file to create, relative to the project root"
					},
					"root_node_type": {
						"type": "string",
						"minLength": 1,
						"description": "The node type of the scene's root node"
					}
				},
				"required": ["file_path", "root_node_type"]
			},
			"annotations": {
				"readOnlyHint": false,
				"destructiveHint": false,
				"idempotentHint": false,
				"openWorldHint": false
			},
			"outputSchema": {
				"type": "object",
				"properties": {
					"success": {
						"type": "boolean",
						"description": "True if the scene was created and opened"
					},
					"error": {
						"type": "string",
						"description": "Why the call failed; present instead of the other properties when it did"
					}
				}
			}
		},
		"open_scene": {
			"title": "Open Scene",
			"toolsets": ["scene"],
			"description": "Opens a scene in the editor.",
			"annotations": {
				"readOnlyHint": true,
				"openWorldHint": false
			},
			"inputSchema": {
				"type": "object",
				"properties": {
					"file_path": {
						"type": "string",
						"minLength": 1,
						"description": "Path to the .tscn file to open, relative to the project root"
					}
				},
				"required": ["file_path"]
			},
			"outputSchema": {
				"type": "object",
				"properties": {
					"success": {
						"type": "boolean",
						"description": "True if the scene was opened"
					},
					"error": {
						"type": "string",
						"description": "Why the call failed; present instead of the other properties when it did"
					}
				}
			}
		},
		"get_node_properties": {
			"title": "Get Node Properties",
			"toolsets": ["scene"],
			"description": [
				"Gets the values of properties on specific nodes in the current scene.",
				"",
				"Only properties whose value differs from the node's default are returned; pass 'include_defaults' as true to also include properties left at their default value.",
				"",
				"Properties that Godot neither saves nor shows in the inspector are left out, since they're derived from other properties (e.g. `global_position` comes from `transform`) or inactive given the node's other properties; ask for one by name (e.g. `MyNode:global_position`) to read it anyway.",
				"",
				"Property values are strings in Godot variant syntax:",
				"- String properties are returned raw, without quotes",
				"- Other types use Godot syntax, e.g. `0.5`, `true`, `Vector2(1, 2)`, `Color(1, 0, 0, 1)`",
				"- A property referencing a saved resource is returned as `Resource(\"res://path/to/file.tres\")`",
				"- A property holding an embedded resource is summarized as `Object(ClassName)`, e.g. `Object(SphereMesh)`",
				"",
				"To inspect the properties of an embedded resource, append a colon-separated property path to the node path: `MyMesh:mesh` returns the properties of the mesh resource, and `MyMesh:mesh:radius` returns just that value.",
				"",
				"If any of the node paths can't be found, an error is returned and no properties are read. If a property path can't be resolved, its value will be an object with an `error` key."
			],
			"annotations": {
				"readOnlyHint": true,
				"openWorldHint": false
			},
			"inputSchema": {
				"type": "object",
				"properties": {
					"node_paths": {
						"type": "array",
						"minItems": 1,
						"description": "The nodes to get properties from",
						"items": {
							"type": "string",
							"description": "The path to the node within the scene, relative to the scene root. May include a colon-separated property path (e.g. `MyMesh:mesh`) to inspect a resource or other sub-object."
						}
					},
					"include_defaults": {
						"type": "boolean",
						"description": "Also include properties left at their default value (default: false)"
					}
				},
				"required": ["node_paths"]
			},
			"outputSchema": {
				"type": "object",
				"description": "Maps each requested node path to its result.",
				"properties": {
					"error": {
						"type": "string",
						"description": "Why the call failed; present instead of the other properties when it did"
					}
				},
				"additionalProperties": {
					"oneOf": [
						{
							"type": "object",
							"description": "A map of property names to values (in Godot variant syntax), or an object with an 'error' key if a property path couldn't be resolved.",
							"additionalProperties": {
								"type": "string"
							}
						},
						{
							"type": "string",
							"description": "A single property value (in Godot variant syntax), when the node path resolved to a scalar via a colon-separated property path."
						}
					]
				}
			}
		},
		"set_node_properties": {
			"title": "Set Node Properties",
			"toolsets": ["scene"],
			"description": [
				"Sets the values of properties on specific nodes in the current scene.",
				"",
				"Only pass the properties you wish to change.",
				"EXAMPLE: {\"action\": \"Move node\", \"nodes\": {\"path/to/node\": {\"position\": \"Vector2(1, 2)\"}}}",
				"",
				"Property values are strings in Godot variant syntax:",
				"- String properties take the raw string, with NO extra quotes (e.g. \"text\": \"Hello\")",
				"- Other types use Godot syntax, e.g. `0.5`, `true`, `Vector2(1, 2)`, `Color(1, 0, 0, 1)`, `NodePath(\"../Other\")`",
				"- To reference a saved resource: `Resource(\"res://path/to/file.tres\")`",
				"- To create an embedded resource: `Object(ClassName, \"prop\": value, ...)`, e.g. `Object(SphereMesh,\"radius\":2.0)`, or `Object(SphereMesh)` for one with every property at its default. Do NOT use SubResource() or ExtResource() - those only work inside .tscn files.",
				"- Careful: `Object(ClassName)` is also how an existing embedded resource reads back, so passing that in replaces it with a new one, losing whatever it held.",
				"",
				"Property names may use colon-separated sub-paths to set properties nested inside resources or other properties, e.g. `mesh:radius`, `material:albedo_color`, `position:x`.",
				"",
				"Note: changing a sub-property of a saved resource (`Resource(\"res://...\")`) affects every scene using that resource.",
				"",
				"This uses the Godot editor's undo/redo system to set the properties, so as many set operations as possible should be done in a single call, so they can all be undone at once.",
				"",
				"On error, nothing is changed.",
				"",
				"Note: Just because this returns successfully, doesn't mean all properties were able to be set to the requested value. Always check that the properties have the correct value afterwards."
			],
			"annotations": {
				"readOnlyHint": false,
				"destructiveHint": true,
				"idempotentHint": true,
				"openWorldHint": false
			},
			"inputSchema": {
				"type": "object",
				"properties": {
					"action": {
						"type": "string",
						"minLength": 1,
						"description": "Human-readable description of the action that will be shown in Godot's undo/redo history"
					},
					"nodes": {
						"type": "object",
						"minProperties": 1,
						"description": "Keys are the paths of the nodes to change, within the scene and relative to its root. A key may include a colon-separated property path (e.g. `MyMesh:mesh`) to address a resource or other sub-object.",
						"additionalProperties": {
							"type": "object",
							"description": "Keys are the Godot property names to set (colon-separated sub-paths allowed, e.g. `mesh:radius`)",
							"additionalProperties": {
								"type": "string",
								"description": "The property value as a string, in Godot variant syntax (see the tool description)"
							}
						}
					}
				},
				"required": ["action", "nodes"]
			},
			"outputSchema": {
				"type": "object",
				"properties": {
					"success": {
						"type": "boolean",
						"description": "True if the properties were set. Note: this doesn't guarantee every property took the requested value."
					},
					"error": {
						"type": "string",
						"description": "Why the call failed; present instead of the other properties when it did"
					}
				}
			}
		},
		"add_node": {
			"title": "Add Node",
			"toolsets": ["scene"],
			"description": [
				"Creates a new node in the current scene, with the given properties set.",
				"",
				"Property values are strings in Godot variant syntax:",
				"- String properties take the raw string, with NO extra quotes (e.g. \"name\": \"MyNode\")",
				"- Other types use Godot syntax, e.g. `0.5`, `true`, `Vector2(1, 2)`, `Color(1, 0, 0, 1)`, `NodePath(\"../Other\")`",
				"- To reference a saved resource: `Resource(\"res://path/to/file.tres\")`",
				"- To create an embedded resource: `Object(ClassName, \"prop\": value, ...)`, e.g. `Object(SphereMesh,\"radius\":2.0)`, or `Object(SphereMesh)` for one with every property at its default. Do NOT use SubResource() or ExtResource() - those only work inside .tscn files.",
				"",
				"Note: Just because this returns successfully, doesn't mean all properties were able to be set to the requested value. Always check that the properties have the correct value afterwards."
			],
			"annotations": {
				"readOnlyHint": false,
				"destructiveHint": false,
				"idempotentHint": false,
				"openWorldHint": false
			},
			"inputSchema": {
				"type": "object",
				"properties": {
					"parent_path": {
						"type": "string",
						"minLength": 1,
						"description": "Path to the parent node, relative the scene root"
					},
					"node_type": {
						"type": "string",
						"minLength": 1,
						"description": "The name of the node class to create"
					},
					"properties": {
						"type": "object",
						"description": "Keys are the Godot property names to set",
						"additionalProperties": {
							"type": "string",
							"description": "The property value as a string, in Godot variant syntax (see the tool description)"
						}
					}
				},
				"required": ["parent_path", "node_type"]
			},
			"outputSchema": {
				"type": "object",
				"properties": {
					"success": {
						"type": "boolean",
						"description": "True if the node was created"
					},
					"node_path": {
						"type": "string",
						"description": "The path of the new node, relative to the scene root"
					},
					"error": {
						"type": "string",
						"description": "Why the call failed; present instead of the other properties when it did"
					}
				}
			}
		},
		"remove_node": {
			"title": "Remove Node",
			"toolsets": ["scene"],
			"description": "Removes the given node (and all its children) from the current scene.",
			"annotations": {
				"readOnlyHint": false,
				"destructiveHint": true,
				"idempotentHint": false,
				"openWorldHint": false
			},
			"inputSchema": {
				"type": "object",
				"properties": {
					"node_path": {
						"type": "string",
						"minLength": 1,
						"description": "Path to the node, relative to the scene root"
					}
				},
				"required": ["node_path"]
			},
			"outputSchema": {
				"type": "object",
				"properties": {
					"success": {
						"type": "boolean",
						"description": "True if the node was removed"
					},
					"error": {
						"type": "string",
						"description": "Why the call failed; present instead of the other properties when it did"
					}
				}
			}
		},
		"create_resource": {
			"title": "Create Resource",
			"toolsets": ["resource"],
			"description": [
				"Creates a new resource.",
				"",
				"Does NOT open it automatically.",
				"",
				"Property values are strings in Godot variant syntax:",
				"- String properties take the raw string, with NO extra quotes (e.g. \"resource_name\": \"My Resource\")",
				"- Other types use Godot syntax, e.g. `0.5`, `true`, `Vector2(1, 2)`, `Color(1, 0, 0, 1)`",
				"- To reference a saved resource: `Resource(\"res://path/to/file.tres\")`",
				"- To create an embedded resource: `Object(ClassName, \"prop\": value, ...)`, e.g. `Object(SphereMesh,\"radius\":2.0)`, or `Object(SphereMesh)` for one with every property at its default. Do NOT use SubResource() or ExtResource() - those only work inside .tscn files."
			],
			"annotations": {
				"readOnlyHint": false,
				"destructiveHint": false,
				"idempotentHint": false,
				"openWorldHint": false
			},
			"inputSchema": {
				"type": "object",
				"properties": {
					"file_path": {
						"type": "string",
						"minLength": 1,
						"description": "Path to the .tres file to create, relative to the project root"
					},
					"resource_type": {
						"type": "string",
						"minLength": 1,
						"description": "The resource type"
					},
					"properties": {
						"type": "object",
						"description": "Keys are the Godot property names to set",
						"additionalProperties": {
							"type": "string",
							"description": "The property value as a string, in Godot variant syntax (see the tool description)"
						}
					}
				},
				"required": ["file_path", "resource_type"]
			},
			"outputSchema": {
				"type": "object",
				"properties": {
					"success": {
						"type": "boolean",
						"description": "True if the resource was created and saved"
					},
					"error": {
						"type": "string",
						"description": "Why the call failed; present instead of the other properties when it did"
					}
				}
			}
		},
		"open_resource": {
			"title": "Open Resource",
			"toolsets": ["resource"],
			"description": "Opens a resource in the inspector.",
			"annotations": {
				"readOnlyHint": true,
				"openWorldHint": false
			},
			"inputSchema": {
				"type": "object",
				"properties": {
					"file_path": {
						"type": "string",
						"minLength": 1,
						"description": "Path to the .tres file to open, relative to the project root"
					}
				},
				"required": ["file_path"]
			},
			"outputSchema": {
				"type": "object",
				"properties": {
					"success": {
						"type": "boolean",
						"description": "True if the resource was opened in the inspector"
					},
					"error": {
						"type": "string",
						"description": "Why the call failed; present instead of the other properties when it did"
					}
				}
			}
		},
		"get_resource_properties": {
			"title": "Get Resource Properties",
			"toolsets": ["resource"],
			"description": [
				"Gets the values of properties on a saved resource file (e.g. a .tres file).",
				"",
				"Only properties whose value differs from the resource's default are returned; pass 'include_defaults' as true to also include properties left at their default value.",
				"",
				"Properties that Godot neither saves nor shows in the inspector are left out, since they're inactive given the resource's other properties (e.g. `alpha_scissor_threshold` on a material that isn't using alpha scissor); name one in 'properties' to read it anyway.",
				"",
				"Property values are strings in Godot variant syntax:",
				"- String properties are returned raw, without quotes",
				"- Other types use Godot syntax, e.g. `0.5`, `true`, `Vector2(1, 2)`, `Color(1, 0, 0, 1)`",
				"- A property referencing another saved resource is returned as `Resource(\"res://path/to/file.tres\")`",
				"- A property holding an embedded resource is summarized as `Object(ClassName)`, e.g. `Object(FastNoiseLite)`",
				"",
				"Requested properties may use colon-separated sub-paths to address properties nested inside embedded resources or other properties, e.g. `noise:frequency` or `albedo_color:r`.",
				"",
				"If a requested property holds an embedded resource (e.g. `noise`), all of that resource's properties are returned for it, instead of just a summary.",
				"If a requested property can't be resolved, its value will be an object with an `error` key."
			],
			"annotations": {
				"readOnlyHint": true,
				"openWorldHint": false
			},
			"inputSchema": {
				"type": "object",
				"properties": {
					"file_path": {
						"type": "string",
						"minLength": 1,
						"description": "Path to the resource file, relative to the project root"
					},
					"properties": {
						"type": "array",
						"items": {
							"type": "string",
							"description": "The name of a property to get (colon-separated sub-paths allowed, e.g. `albedo_color:r`)"
						},
						"description": "The properties to get. If omitted, all of the resource's properties are returned."
					},
					"include_defaults": {
						"type": "boolean",
						"description": "Also include properties left at their default value (default: false)"
					}
				},
				"required": ["file_path"]
			},
			"outputSchema": {
				"type": "object",
				"description": "If 'properties' was omitted, a map of all the resource's property names to values. If 'properties' was given, a map of each requested property path to its result.",
				"properties": {
					"error": {
						"type": "string",
						"description": "Why the call failed; present instead of the other properties when it did"
					}
				},
				"additionalProperties": {
					"oneOf": [
						{
							"type": "object",
							"description": "A map of property names to values (in Godot variant syntax) - used both for the whole resource and for a requested property that holds an embedded resource - or an object with an 'error' key if the property path couldn't be resolved.",
							"additionalProperties": {
								"type": "string"
							}
						},
						{
							"type": "string",
							"description": "A single property value, in Godot variant syntax"
						}
					]
				}
			}
		},
		"set_resource_properties": {
			"title": "Set Resource Properties",
			"toolsets": ["resource"],
			"description": [
				"Sets the values of properties on a saved resource file (.tres or .res), and saves the changes to the file.",
				"",
				"Only pass the properties you wish to change.",
				"EXAMPLE: {\"action\": \"Change color\", \"file_path\": \"res://thing.tres\", \"properties\": {\"albedo_color\": \"Color(1, 0, 0, 1)\"}}",
				"",
				"Property values are strings in Godot variant syntax:",
				"- String properties take the raw string, with NO extra quotes (e.g. \"resource_name\": \"My Resource\")",
				"- Other types use Godot syntax, e.g. `0.5`, `true`, `Vector2(1, 2)`, `Color(1, 0, 0, 1)`",
				"- To reference another saved resource: `Resource(\"res://path/to/file.tres\")`",
				"- To create an embedded resource: `Object(ClassName, \"prop\": value, ...)`, e.g. `Object(FastNoiseLite,\"frequency\":0.1)`, or `Object(FastNoiseLite)` for one with every property at its default. Do NOT use SubResource() or ExtResource() - those only work inside .tscn files.",
				"- Careful: `Object(ClassName)` is also how an existing embedded resource reads back, so passing that in replaces it with a new one, losing whatever it held.",
				"",
				"Property names may use colon-separated sub-paths to set properties nested inside embedded resources or other properties, e.g. `noise:frequency`, `albedo_color:r`.",
				"",
				"Note: the changes are saved to the file immediately, and affect every scene using the resource.",
				"",
				"This uses the Godot editor's undo/redo system to set the properties, so as many set operations as possible should be done in a single call, so they can all be undone at once (undo also re-saves the file).",
				"",
				"On error, nothing is changed.",
				"",
				"Note: Just because this returns successfully, doesn't mean all properties were able to be set to the requested value. Always check that the properties have the correct value afterwards."
			],
			"annotations": {
				"readOnlyHint": false,
				"destructiveHint": true,
				"idempotentHint": true,
				"openWorldHint": false
			},
			"inputSchema": {
				"type": "object",
				"properties": {
					"action": {
						"type": "string",
						"minLength": 1,
						"description": "Human-readable description of the action that will be shown in Godot's undo/redo history"
					},
					"file_path": {
						"type": "string",
						"minLength": 1,
						"description": "Path to the .tres or .res file, relative to the project root"
					},
					"properties": {
						"type": "object",
						"description": "Keys are the Godot property names to set (colon-separated sub-paths allowed, e.g. `albedo_color:r`)",
						"additionalProperties": {
							"type": "string",
							"description": "The property value as a string, in Godot variant syntax (see the tool description)"
						}
					}
				},
				"required": ["action", "file_path"]
			},
			"outputSchema": {
				"type": "object",
				"properties": {
					"success": {
						"type": "boolean",
						"description": "True if the properties were set and the file was saved. Note: this doesn't guarantee every property was set to the requested value."
					},
					"error": {
						"type": "string",
						"description": "Why the call failed; present instead of the other properties when it did"
					}
				}
			}
		},
		"get_selected_nodes": {
			"title": "Get Selected Nodes",
			"toolsets": ["scene"],
			"description": [
				"Gets the nodes currently selected in the editor for the current scene.",
				"",
				"If nothing is selected, 'node_paths' is an empty array."
			],
			"annotations": {
				"readOnlyHint": true,
				"openWorldHint": false
			},
			"outputSchema": {
				"type": "object",
				"properties": {
					"node_paths": {
						"type": "array",
						"description": "Paths of the selected nodes, relative to the scene root",
						"items": {
							"type": "string"
						}
					},
					"error": {
						"type": "string",
						"description": "Why the call failed; present instead of the other properties when it did"
					}
				}
			}
		},
		"instantiate_scene": {
			"title": "Instantiate Scene",
			"toolsets": ["scene"],
			"description": "Instantiates a saved scene (.tscn/.scn) as a child node in the current scene, like adding an instanced scene in the editor.",
			"annotations": {
				"readOnlyHint": false,
				"destructiveHint": false,
				"idempotentHint": false,
				"openWorldHint": false
			},
			"inputSchema": {
				"type": "object",
				"properties": {
					"parent_path": {
						"type": "string",
						"minLength": 1,
						"description": "Path to the parent node, relative to the scene root"
					},
					"scene_path": {
						"type": "string",
						"minLength": 1,
						"description": "Path to the scene file to instantiate, relative to the project root"
					},
					"name": {
						"type": "string",
						"description": "Name for the new instance node"
					}
				},
				"required": ["parent_path", "scene_path"]
			},
			"outputSchema": {
				"type": "object",
				"properties": {
					"success": {
						"type": "boolean",
						"description": "True if the scene was instantiated"
					},
					"node_path": {
						"type": "string",
						"description": "The path of the new instance node, relative to the scene root"
					},
					"error": {
						"type": "string",
						"description": "Why the call failed; present instead of the other properties when it did"
					}
				}
			}
		},
		"save_scene": {
			"title": "Save Scene",
			"toolsets": ["scene"],
			"description": [
				"Saves the scene currently open in the editor to its file.",
				"",
				"If the scene has never been saved, use save_scene_as instead."
			],
			"annotations": {
				"readOnlyHint": false,
				"destructiveHint": true,
				"idempotentHint": false,
				"openWorldHint": false
			},
			"outputSchema": {
				"type": "object",
				"properties": {
					"success": {
						"type": "boolean",
						"description": "True if the scene was saved"
					},
					"scene_path": {
						"type": "string",
						"description": "The path the scene was saved to"
					},
					"error": {
						"type": "string",
						"description": "Why the call failed; present instead of the other properties when it did"
					}
				}
			}
		},
		"save_scene_as": {
			"title": "Save Scene As",
			"toolsets": ["scene"],
			"description": "Saves the scene currently open in the editor to the given path, changing the path of the current scene, such that subsequent save_scene calls would save to the new path.",
			"annotations": {
				"readOnlyHint": false,
				"destructiveHint": false,
				"idempotentHint": false,
				"openWorldHint": false
			},
			"inputSchema": {
				"type": "object",
				"properties": {
					"file_path": {
						"type": "string",
						"minLength": 1,
						"description": "Path to the .tscn file to save to, relative to the project root"
					}
				},
				"required": ["file_path"]
			},
			"outputSchema": {
				"type": "object",
				"properties": {
					"success": {
						"type": "boolean",
						"description": "True if the scene was saved"
					},
					"scene_path": {
						"type": "string",
						"description": "The path the scene was saved to"
					},
					"error": {
						"type": "string",
						"description": "Why the call failed; present instead of the other properties when it did"
					}
				}
			}
		},
		"add_to_group": {
			"title": "Add Node to Groups",
			"toolsets": ["scene"],
			"description": "Adds a node in the current scene to one or more groups.",
			"annotations": {
				"readOnlyHint": false,
				"destructiveHint": true,
				"idempotentHint": true,
				"openWorldHint": false
			},
			"inputSchema": {
				"type": "object",
				"properties": {
					"node_path": {
						"type": "string",
						"minLength": 1,
						"description": "Path to the node, relative to the scene root"
					},
					"groups": {
						"type": "array",
						"minItems": 1,
						"items": {
							"type": "string",
							"description": "A group name"
						},
						"description": "The groups to add the node to"
					}
				},
				"required": ["node_path", "groups"]
			},
			"outputSchema": {
				"type": "object",
				"properties": {
					"success": {
						"type": "boolean",
						"description": "True if the node was added to the groups"
					},
					"error": {
						"type": "string",
						"description": "Why the call failed; present instead of the other properties when it did"
					}
				}
			}
		},
		"remove_from_group": {
			"title": "Remove Node from Groups",
			"toolsets": ["scene"],
			"description": "Removes a node in the current scene from one or more groups.",
			"annotations": {
				"readOnlyHint": false,
				"destructiveHint": true,
				"idempotentHint": true,
				"openWorldHint": false
			},
			"inputSchema": {
				"type": "object",
				"properties": {
					"node_path": {
						"type": "string",
						"minLength": 1,
						"description": "Path to the node, relative to the scene root"
					},
					"groups": {
						"type": "array",
						"minItems": 1,
						"items": {
							"type": "string",
							"description": "A group name"
						},
						"description": "The groups to remove the node from"
					}
				},
				"required": ["node_path", "groups"]
			},
			"outputSchema": {
				"type": "object",
				"properties": {
					"success": {
						"type": "boolean",
						"description": "True if the node was removed from the groups"
					},
					"error": {
						"type": "string",
						"description": "Why the call failed; present instead of the other properties when it did"
					}
				}
			}
		},
		"get_node_groups": {
			"title": "Get Node Groups",
			"toolsets": ["scene"],
			"description": "Gets the groups that nodes in the current scene belong to.",
			"annotations": {
				"readOnlyHint": true,
				"openWorldHint": false
			},
			"inputSchema": {
				"type": "object",
				"properties": {
					"node_paths": {
						"type": "array",
						"minItems": 1,
						"description": "The nodes to get the groups of",
						"items": {
							"type": "string",
							"description": "Path to a node, relative to the scene root"
						}
					}
				},
				"required": ["node_paths"]
			},
			"outputSchema": {
				"type": "object",
				"description": "Maps each requested node path to an array of its group names (internal engine groups omitted).",
				"properties": {
					"error": {
						"type": "string",
						"description": "Why the call failed; present instead of the other properties when it did"
					}
				},
				"additionalProperties": {
					"type": "array",
					"items": {
						"type": "string"
					}
				}
			}
		},
		"connect_signal": {
			"title": "Connect Signal",
			"toolsets": ["scene"],
			"description": [
				"Connects a signal on one node to a method on another node, in the current scene.",
				"",
				"Both nodes must be in the current scene. The signal must exist on 'from_node', and the method on 'to_node'."
			],
			"annotations": {
				"readOnlyHint": false,
				"destructiveHint": true,
				"idempotentHint": false,
				"openWorldHint": false
			},
			"inputSchema": {
				"type": "object",
				"properties": {
					"from_node": {
						"type": "string",
						"minLength": 1,
						"description": "Path to the node emitting the signal, relative to the scene root"
					},
					"signal": {
						"type": "string",
						"minLength": 1,
						"description": "Name of the signal to connect"
					},
					"to_node": {
						"type": "string",
						"minLength": 1,
						"description": "Path to the node receiving the signal, relative to the scene root"
					},
					"method": {
						"type": "string",
						"minLength": 1,
						"description": "Name of the method to call on the receiving node"
					}
				},
				"required": ["from_node", "signal", "to_node", "method"]
			},
			"outputSchema": {
				"type": "object",
				"properties": {
					"success": {
						"type": "boolean",
						"description": "True if the signal was connected"
					},
					"error": {
						"type": "string",
						"description": "Why the call failed; present instead of the other properties when it did"
					}
				}
			}
		},
		"disconnect_signal": {
			"title": "Disconnect Signal",
			"toolsets": ["scene"],
			"description": "Disconnects a previously-connected signal between two nodes in the current scene.",
			"annotations": {
				"readOnlyHint": false,
				"destructiveHint": true,
				"idempotentHint": false,
				"openWorldHint": false
			},
			"inputSchema": {
				"type": "object",
				"properties": {
					"from_node": {
						"type": "string",
						"minLength": 1,
						"description": "Path to the node emitting the signal, relative to the scene root"
					},
					"signal": {
						"type": "string",
						"minLength": 1,
						"description": "Name of the signal to disconnect"
					},
					"to_node": {
						"type": "string",
						"minLength": 1,
						"description": "Path to the node receiving the signal, relative to the scene root"
					},
					"method": {
						"type": "string",
						"minLength": 1,
						"description": "Name of the connected method on the receiving node"
					}
				},
				"required": ["from_node", "signal", "to_node", "method"]
			},
			"outputSchema": {
				"type": "object",
				"properties": {
					"success": {
						"type": "boolean",
						"description": "True if the signal was disconnected"
					},
					"error": {
						"type": "string",
						"description": "Why the call failed; present instead of the other properties when it did"
					}
				}
			}
		},
		"attach_script": {
			"title": "Attach Script",
			"toolsets": ["scene"],
			"description": [
				"Attaches an existing script file to a node in the current scene.",
				"",
				"The script's base type must be compatible with the node's type.",
				"",
				"Use create_script first if the script doesn't exist yet."
			],
			"annotations": {
				"readOnlyHint": false,
				"destructiveHint": true,
				"idempotentHint": true,
				"openWorldHint": false
			},
			"inputSchema": {
				"type": "object",
				"properties": {
					"node_path": {
						"type": "string",
						"minLength": 1,
						"description": "Path to the node, relative to the scene root"
					},
					"script_path": {
						"type": "string",
						"minLength": 1,
						"description": "Path to the script file, relative to the project root"
					}
				},
				"required": ["node_path", "script_path"]
			},
			"outputSchema": {
				"type": "object",
				"properties": {
					"success": {
						"type": "boolean",
						"description": "True if the script was attached"
					},
					"error": {
						"type": "string",
						"description": "Why the call failed; present instead of the other properties when it did"
					}
				}
			}
		},
		"detach_script": {
			"title": "Detach Script",
			"toolsets": ["scene"],
			"description": "Removes the script attached to a node in the current scene.",
			"annotations": {
				"readOnlyHint": false,
				"destructiveHint": true,
				"idempotentHint": false,
				"openWorldHint": false
			},
			"inputSchema": {
				"type": "object",
				"properties": {
					"node_path": {
						"type": "string",
						"minLength": 1,
						"description": "Path to the node, relative to the scene root"
					}
				},
				"required": ["node_path"]
			},
			"outputSchema": {
				"type": "object",
				"properties": {
					"success": {
						"type": "boolean",
						"description": "True if the script was detached"
					},
					"error": {
						"type": "string",
						"description": "Why the call failed; present instead of the other properties when it did"
					}
				}
			}
		},
		"create_script": {
			"title": "Create Script",
			"toolsets": ["script"],
			"description": [
				"Creates a new GDScript file.",
				"",
				"Does NOT open or attach it.",
				"",
				"If 'content' is given, it is used as the file's contents. Otherwise, a minimal script extending 'base_class' is generated."
			],
			"annotations": {
				"readOnlyHint": false,
				"destructiveHint": false,
				"idempotentHint": false,
				"openWorldHint": false
			},
			"inputSchema": {
				"type": "object",
				"properties": {
					"file_path": {
						"type": "string",
						"minLength": 1,
						"description": "Path to the .gd file to create, relative to the project root"
					},
					"base_class": {
						"type": "string",
						"description": "Base class for the generated script when 'content' is omitted (default: Node)"
					},
					"content": {
						"type": "string",
						"description": "Full contents of the script file. If omitted, a minimal template is generated."
					}
				},
				"required": ["file_path"]
			},
			"outputSchema": {
				"type": "object",
				"properties": {
					"success": {
						"type": "boolean",
						"description": "True if the script file was created"
					},
					"error": {
						"type": "string",
						"description": "Why the call failed; present instead of the other properties when it did"
					}
				}
			}
		},
		"open_script": {
			"title": "Open Script",
			"toolsets": ["script"],
			"description": "Opens a script file in the editor's script editor.",
			"annotations": {
				"readOnlyHint": true,
				"openWorldHint": false
			},
			"inputSchema": {
				"type": "object",
				"properties": {
					"file_path": {
						"type": "string",
						"minLength": 1,
						"description": "Path to the .gd file to open, relative to the project root"
					}
				},
				"required": ["file_path"]
			},
			"outputSchema": {
				"type": "object",
				"properties": {
					"success": {
						"type": "boolean",
						"description": "True if the script was opened in the script editor"
					},
					"error": {
						"type": "string",
						"description": "Why the call failed; present instead of the other properties when it did"
					}
				}
			}
		},
		"save_script": {
			"title": "Save Script",
			"toolsets": ["script"],
			"description": [
				"Saves a script's unsaved changes from the editor to its file.",
				"",
				"If the script is open in the editor, its current (possibly unsaved) contents are written to disk. If it isn't open, there is nothing to save and this is a no-op."
			],
			"annotations": {
				"readOnlyHint": false,
				"destructiveHint": true,
				"idempotentHint": false,
				"openWorldHint": false
			},
			"inputSchema": {
				"type": "object",
				"properties": {
					"file_path": {
						"type": "string",
						"minLength": 1,
						"description": "Path to the .gd file, relative to the project root"
					}
				},
				"required": ["file_path"]
			},
			"outputSchema": {
				"type": "object",
				"properties": {
					"success": {
						"type": "boolean",
						"description": "True if the operation succeeded"
					},
					"saved": {
						"type": "boolean",
						"description": "True if the script was open and its contents were written to disk; false if it wasn't open (nothing to save)"
					},
					"error": {
						"type": "string",
						"description": "Why the call failed; present instead of the other properties when it did"
					}
				}
			}
		},
		"read_script": {
			"title": "Read Script",
			"toolsets": ["script"],
			"description": [
				"Reads the contents of a script file.",
				"",
				"If the script is currently open in the editor, its live (possibly unsaved) buffer is returned and 'open_in_editor' is true. Otherwise the contents are read from disk."
			],
			"annotations": {
				"readOnlyHint": true,
				"openWorldHint": false
			},
			"inputSchema": {
				"type": "object",
				"properties": {
					"file_path": {
						"type": "string",
						"minLength": 1,
						"description": "Path to the .gd file, relative to the project root"
					}
				},
				"required": ["file_path"]
			},
			"outputSchema": {
				"type": "object",
				"properties": {
					"content": {
						"type": "string",
						"description": "The contents of the script (the live editor buffer if open, otherwise the file on disk)"
					},
					"open_in_editor": {
						"type": "boolean",
						"description": "True if the script is currently open in the editor (so 'content' is the live buffer)"
					},
					"error": {
						"type": "string",
						"description": "Why the call failed; present instead of the other properties when it did"
					}
				}
			}
		},
		"write_script": {
			"title": "Write Script",
			"toolsets": ["script"],
			"description": [
				"Replaces the entire contents of an existing script file.",
				"",
				"You must read the script with read_script first: this fails if the script has changed since you last read it (or created/wrote it), so you don't overwrite edits you haven't seen. If it fails for that reason, read the script again and reapply your change.",
				"",
				"If the script is open in the editor, its editor buffer is updated instead of the file on disk (so unsaved edits aren't silently clobbered); 'saved' will be false, and you can call save_script to write it to disk. If it isn't open, the file is written directly and 'saved' is true.",
				"",
				"Use create_script to make a new script."
			],
			"annotations": {
				"readOnlyHint": false,
				"destructiveHint": true,
				"idempotentHint": true,
				"openWorldHint": false
			},
			"inputSchema": {
				"type": "object",
				"properties": {
					"file_path": {
						"type": "string",
						"minLength": 1,
						"description": "Path to the .gd file, relative to the project root"
					},
					"content": {
						"type": "string",
						"description": "The full new contents of the script, which may be empty to blank the script out"
					}
				},
				"required": ["file_path", "content"]
			},
			"outputSchema": {
				"type": "object",
				"properties": {
					"success": {
						"type": "boolean",
						"description": "True if the script was written"
					},
					"open_in_editor": {
						"type": "boolean",
						"description": "True if the script was open, so its editor buffer was updated rather than the file on disk"
					},
					"saved": {
						"type": "boolean",
						"description": "True if the new contents were written to disk; false if only the editor buffer was updated (call save_script to flush it)"
					},
					"error": {
						"type": "string",
						"description": "Why the call failed; present instead of the other properties when it did"
					}
				}
			}
		},
		"run_project": {
			"title": "Run Project",
			"toolsets": ["project"],
			"description": [
				"Runs the project from the editor.",
				"",
				"'scene' controls what runs: omit it or use 'main' to run the project's main scene, 'current' to run the scene open in the editor, or a path to run a specific scene file."
			],
			"annotations": {
				"readOnlyHint": false,
				"destructiveHint": true,
				"idempotentHint": false,
				"openWorldHint": false
			},
			"inputSchema": {
				"type": "object",
				"properties": {
					"scene": {
						"type": "string",
						"description": "What to run: 'main' (default), 'current', or a path to a scene file"
					},
					"clear_log_messages": {
						"type": "boolean",
						"description": "Clear the captured log messages before running, so get_log_messages only returns output from this run onward (default: false)"
					}
				}
			},
			"outputSchema": {
				"type": "object",
				"properties": {
					"success": {
						"type": "boolean",
						"description": "True if the project started running"
					},
					"error": {
						"type": "string",
						"description": "Why the call failed; present instead of the other properties when it did"
					}
				}
			}
		},
		"stop_project": {
			"title": "Stop Project",
			"toolsets": ["project"],
			"description": "Stops the running project, if any.",
			"annotations": {
				"readOnlyHint": false,
				"destructiveHint": false,
				"idempotentHint": true,
				"openWorldHint": false
			},
			"outputSchema": {
				"type": "object",
				"properties": {
					"success": {
						"type": "boolean",
						"description": "True (the running project, if any, was stopped)"
					},
					"error": {
						"type": "string",
						"description": "Why the call failed; present instead of the other properties when it did"
					}
				}
			}
		},
		"reimport": {
			"title": "Reimport Assets",
			"toolsets": ["import"],
			"description": [
				"Reimports one or more asset files (e.g. after changing the source file or its import settings).",
				"",
				"Each path must be an imported asset (i.e. have a .import file).",
				"",
				"On error, nothing is reimported."
			],
			"annotations": {
				"readOnlyHint": false,
				"destructiveHint": true,
				"idempotentHint": false,
				"openWorldHint": false
			},
			"inputSchema": {
				"type": "object",
				"properties": {
					"file_paths": {
						"type": "array",
						"minItems": 1,
						"description": "The assets to reimport",
						"items": {
							"type": "string",
							"description": "Path to an imported asset, relative to the project root"
						}
					}
				},
				"required": ["file_paths"]
			},
			"outputSchema": {
				"type": "object",
				"properties": {
					"success": {
						"type": "boolean",
						"description": "True if the assets were reimported"
					},
					"error": {
						"type": "string",
						"description": "Why the call failed; present instead of the other properties when it did"
					}
				}
			}
		},
		"get_import_settings": {
			"title": "Get Import Settings",
			"toolsets": ["import"],
			"description": [
				"Gets the import settings for an imported asset (e.g. a texture or audio file).",
				"",
				"Option values are strings in Godot variant syntax."
			],
			"annotations": {
				"readOnlyHint": true,
				"openWorldHint": false
			},
			"inputSchema": {
				"type": "object",
				"properties": {
					"file_path": {
						"type": "string",
						"minLength": 1,
						"description": "Path to the imported asset, relative to the project root"
					}
				},
				"required": ["file_path"]
			},
			"outputSchema": {
				"type": "object",
				"properties": {
					"importer": {
						"type": "string",
						"description": "The name of the importer used for this asset"
					},
					"options": {
						"type": "object",
						"description": "A map of import option names to values, in Godot variant syntax",
						"additionalProperties": {
							"type": "string"
						}
					},
					"error": {
						"type": "string",
						"description": "Why the call failed; present instead of the other properties when it did"
					}
				}
			}
		},
		"set_import_settings": {
			"title": "Set Import Settings",
			"toolsets": ["import"],
			"description": [
				"Changes the import settings for an imported asset, and reimports it.",
				"",
				"Only pass the options you wish to change.",
				"",
				"Option values are strings in Godot variant syntax (e.g. `2`, `true`, `2.0`); a value that isn't valid variant syntax is taken as a raw string.",
				"",
				"An unknown option name fails the whole call. On error, nothing is changed.",
				"",
				"Use get_import_settings to see the available option names and the current importer."
			],
			"annotations": {
				"readOnlyHint": false,
				"destructiveHint": true,
				"idempotentHint": true,
				"openWorldHint": false
			},
			"inputSchema": {
				"type": "object",
				"properties": {
					"file_path": {
						"type": "string",
						"minLength": 1,
						"description": "Path to the imported asset, relative to the project root"
					},
					"importer": {
						"type": "string",
						"description": "Change the importer used for this asset"
					},
					"options": {
						"type": "object",
						"description": "Import options to set (keys are option names)",
						"additionalProperties": {
							"type": "string",
							"description": "The option value, as a string in Godot variant syntax"
						}
					}
				},
				"required": ["file_path"]
			},
			"outputSchema": {
				"type": "object",
				"properties": {
					"success": {
						"type": "boolean",
						"description": "True if the import settings were changed and the asset was reimported"
					},
					"error": {
						"type": "string",
						"description": "Why the call failed; present instead of the other properties when it did"
					}
				}
			}
		},
		"get_editor_settings": {
			"title": "Get Editor Settings",
			"toolsets": ["editor"],
			"description": [
				"Gets settings from the Godot editor itself (the values stored in the global editor configuration, e.g. 'text_editor/behavior/indent/size' or 'text_editor/appearance/whitespace/draw_tabs'). These are editor-wide preferences, not project settings.",
				"",
				"Pass 'names' to get specific settings by name (returned regardless of whether they match their default).",
				"Otherwise, all settings are returned, but only those whose value differs from the default; pass 'include_defaults' as true to also include settings left at their default value.",
				"",
				"Setting values are strings in Godot variant syntax.",
				"",
				"Settings under 'godai/' are Godai's own configuration and are never returned; only the user can see them, via Editor Settings."
			],
			"annotations": {
				"readOnlyHint": true,
				"openWorldHint": false
			},
			"inputSchema": {
				"type": "object",
				"properties": {
					"names": {
						"type": "array",
						"description": "Specific setting names to get. If omitted, all settings are returned.",
						"items": {
							"type": "string"
						}
					},
					"include_defaults": {
						"type": "boolean",
						"description": "When getting all settings, also include those left at their default value (default: false)"
					}
				}
			},
			"outputSchema": {
				"type": "object",
				"properties": {
					"settings": {
						"type": "object",
						"description": "A map of setting name to value, in Godot variant syntax",
						"additionalProperties": {
							"type": "string"
						}
					},
					"error": {
						"type": "string",
						"description": "Why the call failed; present instead of the other properties when it did"
					}
				}
			}
		},
		"set_editor_settings": {
			"title": "Set Editor Settings",
			"toolsets": ["editor"],
			"description": [
				"Changes settings in the Godot editor itself (editor-wide preferences, not project settings).",
				"",
				"Pass 'settings': a map of setting name (e.g. 'text_editor/behavior/indent/size') to value. Values are strings in Godot variant syntax (e.g. `16`, `true`, `2.0`); a value that isn't valid variant syntax is taken as a raw string.",
				"",
				"Use get_editor_settings to see the available setting names and their current values.",
				"",
				"Settings under 'godai/' are Godai's own configuration and can't be changed; only the user can, via Editor Settings."
			],
			"annotations": {
				"readOnlyHint": false,
				"destructiveHint": true,
				"idempotentHint": true,
				"openWorldHint": false
			},
			"inputSchema": {
				"type": "object",
				"properties": {
					"settings": {
						"type": "object",
						"minProperties": 1,
						"description": "Settings to set (keys are setting names)",
						"additionalProperties": {
							"type": "string",
							"description": "The setting value, as a string in Godot variant syntax"
						}
					}
				},
				"required": ["settings"]
			},
			"outputSchema": {
				"type": "object",
				"properties": {
					"success": {
						"type": "boolean",
						"description": "True if the settings were changed"
					},
					"error": {
						"type": "string",
						"description": "Why the call failed; present instead of the other properties when it did"
					}
				}
			}
		},
		"get_log_messages": {
			"title": "Get Log Messages",
			"toolsets": ["editor"],
			"description": [
				"Gets recent log output from the editor (print() output, warnings, and errors) captured since the editor started.",
				"",
				"Messages are sorted oldest first. Includes messages from both the editor and games run via `run_project`.",
				"",
				"Each line is prefixed with the local time it was captured, as '[HH:MM:SS.mmm]', so identical lines from different moments can be told apart."
			],
			"annotations": {
				"readOnlyHint": true,
				"openWorldHint": false
			},
			"inputSchema": {
				"type": "object",
				"properties": {
					"count": {
						"type": "integer",
						"description": "Maximum number of recent messages to return (default: 100). Use 0 for all retained messages."
					}
				}
			},
			"outputSchema": {
				"type": "object",
				"properties": {
					"messages": {
						"type": "array",
						"description": "The most recent log lines, oldest first",
						"items": {
							"type": "string"
						}
					},
					"error": {
						"type": "string",
						"description": "Why the call failed; present instead of the other properties when it did"
					}
				}
			}
		},
		"clear_log_messages": {
			"title": "Clear Log Messages",
			"description": [
				"Clears the log messages captured so far, so subsequent get_log_messages calls only return output from after this point.",
				"",
				"This only affects the captured messages; the editor's own Output panel is untouched."
			],
			"annotations": {
				"readOnlyHint": false,
				"destructiveHint": true,
				"idempotentHint": true,
				"openWorldHint": false
			},
			"outputSchema": {
				"type": "object",
				"properties": {
					"success": {
						"type": "boolean",
						"description": "True if the captured log messages were cleared"
					},
					"error": {
						"type": "string",
						"description": "Why the call failed; present instead of the other properties when it did"
					}
				}
			}
		},
		"execute_editor_script": {
			"title": "Execute Editor Script",
			"toolsets": ["editor"],
			"description": [
				"Executes the given GDScript code in the editor, in the context of a Node that is a child of the scene currently being edited.",
				"",
				"For modifying the current scene, use the other tools when possible.",
				"",
				"However, if you do use a script to modify the current scene, you MUST use `EditorUndoRedoManager` from `EditorInterface.get_editor_undo_redo()`, and the action name MUST end with \"(Godai)\".",
				"",
				"You can find nodes relative to the scene root using `EditorInterface.get_edited_scene_root().get_node_or_null(node_path)`.",
				"",
				"Two helper methods have been provided:",
				"- `func editor_undo_redo_create_node(p_undo_redo: EditorUndoRedoManager, p_parent: Node, p_child: Node) -> void`",
				"- `func editor_undo_redo_remove_node(p_undo_redo: EditorUndoRedoManager, p_parent: Node, p_child: Node) -> void`",
				"",
				"If you are using `EditorUndoRedoManager` to add or remove a node, you MUST call one of those helper methods before calling `commit_action()`. This will add some `add_do_method()` and `add_undo_method()` calls to ensure the changes are synchronized to the live game if the game is running.",
				"",
				"If the script successfully runs, the output from `print()` will be returned."
			],
			"annotations": {
				"readOnlyHint": false,
				"destructiveHint": true,
				"idempotentHint": false,
				"openWorldHint": true
			},
			"inputSchema": {
				"type": "object",
				"properties": {
					"code": {
						"type": "string",
						"minLength": 1,
						"description": "A snippet of GDScript code. It should use tabs rather than spaces for indentation. Don't wrap the code in a `_ready()` function."
					}
				},
				"required": ["code"]
			},
			"outputSchema": {
				"type": "object",
				"properties": {
					"success": {
						"type": "boolean",
						"description": "True if the script ran successfully"
					},
					"output": {
						"type": "array",
						"description": "The lines printed by the script via print()",
						"items": {
							"type": "string"
						}
					},
					"error": {
						"type": "string",
						"description": "Why the call failed; present instead of the other properties when it did"
					}
				}
			}
		},
		"restart_editor": {
			"title": "Restart Editor",
			"toolsets": ["editor"],
			"description": [
				"Restarts the Godot editor for the project.",
				"",
				"The user is asked to confirm first, and the call fails if they decline. A headless editor restarts without asking.",
				"",
				"By default, unsaved changes are saved before restarting; pass 'skip_save' as true to discard them instead.",
				"",
				"If the project is running, it is stopped first."
			],
			"annotations": {
				"readOnlyHint": false,
				"destructiveHint": true,
				"idempotentHint": false,
				"openWorldHint": false
			},
			"inputSchema": {
				"type": "object",
				"properties": {
					"skip_save": {
						"type": "boolean",
						"description": "Discard unsaved changes instead of saving them before restarting (default: false)"
					}
				}
			},
			"outputSchema": {
				"type": "object",
				"properties": {
					"success": {
						"type": "boolean",
						"description": "True once the editor has restarted and reconnected"
					},
					"error": {
						"type": "string",
						"description": "Why the call failed; present instead of the other properties when it did"
					}
				}
			}
		},
		"close_editor": {
			"title": "Close Editor",
			"toolsets": ["editor"],
			"description": [
				"Closes the Godot editor for the project.",
				"",
				"The user is asked to confirm first, and the call fails if they decline. A headless editor closes without asking.",
				"",
				"By default, unsaved changes are saved before closing; pass 'skip_save' as true to discard them instead.",
				"",
				"If the project is running, it is stopped first."
			],
			"annotations": {
				"readOnlyHint": false,
				"destructiveHint": true,
				"idempotentHint": false,
				"openWorldHint": false
			},
			"inputSchema": {
				"type": "object",
				"properties": {
					"skip_save": {
						"type": "boolean",
						"description": "Discard unsaved changes instead of saving them before closing (default: false)"
					}
				}
			},
			"outputSchema": {
				"type": "object",
				"properties": {
					"success": {
						"type": "boolean",
						"description": "True once the user has accepted and the editor is closing"
					},
					"error": {
						"type": "string",
						"description": "Why the call failed; present instead of the other properties when it did"
					}
				}
			}
		}
	}
}
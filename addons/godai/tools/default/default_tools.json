{
	"$schema": "./default_tools.schema.json",
	"tools": {
		"get_current_project": {
			"title": "Get Current Project",
			"description": [
				"Gets information about the project that is currently opened in the Godot editor.",
				"",
				"Returns:",
				" - project_path: The path to the current project",
				" - project_name: The project's configured name",
				" - headless: Whether the editor is running with a headless display driver"
			],
			"outputSchema": {
				"type": "object",
				"properties": {
					"project_path": {
						"type": "string",
						"description": "The absolute path to the current project"
					},
					"project_name": {
						"type": "string",
						"description": "The project's configured name"
					},
					"headless": {
						"type": "boolean",
						"description": "Whether the editor is running with a headless display driver"
					}
				},
				"required": ["project_path", "project_name", "headless"]
			},
			"doNotForward": true
		},
		"get_project_settings": {
			"title": "Get Project Settings",
			"description": [
				"Gets settings from the project (the values stored in 'project.godot', e.g. 'application/config/name' or 'display/window/size/viewport_width').",
				"",
				"Pass 'names' to get specific settings by name (returned regardless of whether they match their default).",
				"Otherwise, all settings are returned, but only those whose value differs from the default; pass 'include_defaults' as true to also include settings left at their default value.",
				"",
				"Returns 'settings': a map of setting name to value, in Godot variant syntax."
			],
			"inputSchema": {
				"type": "object",
				"properties": {
					"names": {
						"type": "array",
						"description": "Optional: specific setting names to get. If omitted, all settings are returned.",
						"items": {
							"type": "string"
						}
					},
					"include_defaults": {
						"type": "boolean",
						"description": "When getting all settings, also include those left at their default value (default: false)"
					}
				}
			},
			"outputSchema": {
				"type": "object",
				"properties": {
					"settings": {
						"type": "object",
						"description": "A map of setting name to value, in Godot variant syntax",
						"additionalProperties": {
							"type": "string"
						}
					}
				},
				"required": ["settings"]
			}
		},
		"set_project_settings": {
			"title": "Set Project Settings",
			"description": [
				"Changes settings in the project and saves 'project.godot'.",
				"",
				"Pass 'settings': a map of setting name (e.g. 'display/window/size/viewport_width') to value. Values are strings in Godot variant syntax (e.g. `1280`, `true`, `2.0`); a value that isn't valid variant syntax is taken as a raw string.",
				"Settings that don't exist yet are created. Use get_project_settings to see the available setting names and their current values."
			],
			"inputSchema": {
				"type": "object",
				"properties": {
					"settings": {
						"type": "object",
						"description": "Settings to set (keys are setting names)",
						"additionalProperties": {
							"type": "string",
							"description": "The setting value, as a string in Godot variant syntax"
						}
					}
				},
				"required": ["settings"]
			},
			"outputSchema": {
				"type": "object",
				"properties": {
					"success": {
						"type": "boolean",
						"description": "True if the settings were changed and the project was saved"
					}
				},
				"required": ["success"]
			}
		},
		"get_current_scene": {
			"title": "Get Current Scene",
			"description": [
				"Gets information about the scene that is currently opened in the Godot editor.",
				"",
				"Returns:",
				" - scene_path: The path to the scene file",
				" - root_node_type: The class of the root Node of the scene",
				" - root_node_name: The name of the root Node of the scene",
				"If no scene is currently open, it'll return an empty string for all of the above."
			],
			"outputSchema": {
				"type": "object",
				"properties": {
					"scene_path": {
						"type": "string",
						"description": "The path to the scene file, '[unsaved]' for a never-saved scene, or empty if no scene is open"
					},
					"root_node_type": {
						"type": "string",
						"description": "The class of the root Node, or empty if no scene is open"
					},
					"root_node_name": {
						"type": "string",
						"description": "The name of the root Node, or empty if no scene is open"
					}
				},
				"required": ["scene_path", "root_node_type", "root_node_name"]
			}
		},
		"get_current_scene_tree": {
			"title": "Get Scene Tree",
			"description": [
				"Gets the tree for the scene that is currently opened in the Godot editor.",
				"",
				"Returns a tree of objects representing the nodes in the scene with the following keys",
				" - name: The Node name",
				" - type: The class of the Node",
				" - path: The path of the node within the scene, relative to the scene root",
				" - script: The path to the script file, if this node has a script attached; otherwise, it'll be omitted",
				" - children: An array of objects with the same structure, representing the child nodes; will be omitted if node has no children",
				"If no scene is currently open, it'll return an empty object."
			],
			"outputSchema": {
				"type": "object",
				"description": "A node in the scene tree, or an empty object if no scene is open. Children share this same structure.",
				"properties": {
					"name": {
						"type": "string",
						"description": "The Node name"
					},
					"type": {
						"type": "string",
						"description": "The class of the Node"
					},
					"path": {
						"type": "string",
						"description": "The path of the node within the scene, relative to the scene root"
					},
					"script": {
						"type": "string",
						"description": "The path to the script file, if this node has a script attached; otherwise omitted"
					},
					"children": {
						"type": "array",
						"description": "The child nodes; omitted if the node has no children",
						"items": {
							"$ref": "#"
						}
					}
				}
			}
		},
		"create_scene": {
			"title": "Create Scene",
			"description": "Creates a new scene and opens it in the editor",
			"inputSchema": {
				"type": "object",
				"properties": {
					"file_path": {
						"type": "string",
						"description": "Path to the .tscn file to create, relative to the project root"
					},
					"root_node_type": {
						"type": "string",
						"description": "The node type of the scene's root node"
					}
				}
			},
			"outputSchema": {
				"type": "object",
				"properties": {
					"success": {
						"type": "boolean",
						"description": "True if the scene was created and opened"
					}
				},
				"required": ["success"]
			}
		},
		"open_scene": {
			"title": "Open Scene",
			"description": "Opens a scene in the editor",
			"inputSchema": {
				"type": "object",
				"properties": {
					"file_path": {
						"type": "string",
						"description": "Path to the .tscn file to open, relative to the project root"
					}
				}
			},
			"outputSchema": {
				"type": "object",
				"properties": {
					"success": {
						"type": "boolean",
						"description": "True if the scene was opened"
					}
				},
				"required": ["success"]
			}
		},
		"get_node_properties": {
			"title": "Get Node Properties",
			"description": [
				"Gets the values of properties on specific nodes in the current scene.",
				"",
				"Returns an object with a key for each requested node path, containing an object that maps property names to values.",
				"Property values are strings in Godot variant syntax:",
				" - String properties are returned raw, without quotes",
				" - Other types use Godot syntax, e.g. `0.5`, `true`, `Vector2(1, 2)`, `Color(1, 0, 0, 1)`",
				" - A property referencing a saved resource is returned as `Resource(\"res://path/to/file.tres\")`",
				" - A property holding an embedded resource is summarized as `Object(ClassName)`, e.g. `Object(SphereMesh)`",
				"To inspect the properties of an embedded resource, append a colon-separated property path to the node path: `MyMesh:mesh` returns the properties of the mesh resource, and `MyMesh:mesh:radius` returns just that value.",
				"If a node isn't found, its value will be an empty object. If a property path can't be resolved, its value will be an object with an `error` key."
			],
			"inputSchema": {
				"type": "object",
				"properties": {
					"node_paths": {
						"type": "array",
						"items": {
							"type": "string",
							"description": "The path to the node within the scene, relative to the scene root. May include a colon-separated property path (e.g. `MyMesh:mesh`) to inspect a resource or other sub-object."
						}
					},
					"modified_only": {
						"type": "boolean",
						"description": "If true, only return properties that differ from their default value. Recommended, to keep the result small."
					}
				}
			},
			"outputSchema": {
				"type": "object",
				"description": "Maps each requested node path to its result. Empty object if no scene is open.",
				"additionalProperties": {
					"oneOf": [
						{
							"type": "object",
							"description": "A map of property names to values (in Godot variant syntax), an empty object if the node wasn't found, or an object with an 'error' key if a property path couldn't be resolved.",
							"additionalProperties": {
								"type": "string"
							}
						},
						{
							"type": "string",
							"description": "A single property value (in Godot variant syntax), when the node path resolved to a scalar via a colon-separated property path."
						}
					]
				}
			}
		},
		"set_node_properties": {
			"title": "Set Node Properties",
			"description": [
				"Sets the values of properties on specific nodes in the current scene.",
				"",
				"Only pass in the properties you actually wish to change.",
				"EXAMPLE: {\"action\": \"Move node\", \"nodes\": [{\"node_path\": \"path/to/node\", \"properties\": {\"position\": \"Vector2(1, 2)\"}}]}",
				"Property values are strings in Godot variant syntax:",
				" - String properties take the raw string, with NO extra quotes (e.g. \"text\": \"Hello\")",
				" - Other types use Godot syntax, e.g. `0.5`, `true`, `Vector2(1, 2)`, `Color(1, 0, 0, 1)`, `NodePath(\"../Other\")`",
				" - To reference a saved resource: `Resource(\"res://path/to/file.tres\")`",
				" - To create an embedded resource: `Object(ClassName, \"prop\": value, ...)`, e.g. `Object(SphereMesh,\"radius\":2.0)`. Do NOT use SubResource() or ExtResource() - those only work inside .tscn files.",
				"Property names may use colon-separated sub-paths to set properties nested inside resources or other properties, e.g. `mesh:radius`, `material:albedo_color`, `position:x`.",
				"Note: changing a sub-property of a saved resource (`Resource(\"res://...\")`) affects every scene using that resource.",
				"This uses the Godot editor's undo/redo system to set the properties, so as many set operations as possible should be done in a single call, so they can all be undone at once.",
				"If any property name or value is invalid, an error is returned and NO properties are changed.",
				"Otherwise, returns an object with a key for each node path, containing true if we were able to find the node; otherwise, false.",
				"Note: Just because this returns successfully, doesn't mean all properties were able to be set to the requested value. Always check that the properties have the correct value afterwards."
			],
			"inputSchema": {
				"type": "object",
				"properties": {
					"action": {
						"type": "string",
						"description": "Human-readable description of the action that will be shown in Godot's undo/redo history"
					},
					"nodes": {
						"type": "array",
						"items": {
							"type": "object",
							"properties": {
								"node_path": {
									"type": "string",
									"description": "The path to the node within the scene, relative to the scene root. May include a colon-separated property path (e.g. `MyMesh:mesh`) to address a resource or other sub-object."
								},
								"properties": {
									"type": "object",
									"description": "Keys are the Godot property names to set (colon-separated sub-paths allowed, e.g. `mesh:radius`)",
									"additionalProperties": {
										"type": "string",
										"description": "The property value as a string, in Godot variant syntax (see the tool description)"
									}
								}
							}
						}
					}
				}
			},
			"outputSchema": {
				"type": "object",
				"description": "Maps each node path to whether the node was found. Empty object if no scene is open. Note: a true value doesn't guarantee every property was set to the requested value.",
				"additionalProperties": {
					"type": "boolean"
				}
			}
		},
		"add_node": {
			"title": "Add Node",
			"description": [
				"Creates a new node in the current scene, with the given properties set.",
				"",
				"Property values are strings in Godot variant syntax:",
				" - String properties take the raw string, with NO extra quotes (e.g. \"name\": \"MyNode\")",
				" - Other types use Godot syntax, e.g. `0.5`, `true`, `Vector2(1, 2)`, `Color(1, 0, 0, 1)`, `NodePath(\"../Other\")`",
				" - To reference a saved resource: `Resource(\"res://path/to/file.tres\")`",
				" - To create an embedded resource: `Object(ClassName, \"prop\": value, ...)`, e.g. `Object(SphereMesh,\"radius\":2.0)`. Do NOT use SubResource() or ExtResource() - those only work inside .tscn files.",
				"If any property name or value is invalid, an error is returned and the node is NOT created.",
				"Otherwise, returns true and the path of the new node.",
				"Note: Just because this returns successfully, doesn't mean all properties were able to be set to the requested value. Always check that the properties have the correct value afterwards."
			],
			"inputSchema": {
				"type": "object",
				"properties": {
					"parent_path": {
						"type": "string",
						"description": "Path to the parent node, relative the scene root"
					},
					"node_type": {
						"type": "string",
						"description": "The name of the node class to create"
					},
					"properties": {
						"type": "object",
						"description": "Keys are the Godot property names to set",
						"additionalProperties": {
							"type": "string",
							"description": "The property value as a string, in Godot variant syntax (see the tool description)"
						}
					}
				}
			},
			"outputSchema": {
				"type": "object",
				"properties": {
					"success": {
						"type": "boolean",
						"description": "True if the node was created"
					},
					"node_path": {
						"type": "string",
						"description": "The path of the new node, relative to the scene root"
					}
				},
				"required": ["success", "node_path"]
			}
		},
		"remove_node": {
			"title": "Remove Node",
			"description": "Removes the given node (and all its children) from the current scene",
			"inputSchema": {
				"type": "object",
				"properties": {
					"node_path": {
						"type": "string",
						"description": "Path to the node, relative to the scene root"
					}
				}
			},
			"outputSchema": {
				"type": "object",
				"properties": {
					"success": {
						"type": "boolean",
						"description": "True if the node was removed"
					}
				},
				"required": ["success"]
			}
		},
		"create_resource": {
			"title": "Create Resource",
			"description": [
				"Creates a new resource. Does NOT open it automatically.",
				"",
				"Property values are strings in Godot variant syntax:",
				" - String properties take the raw string, with NO extra quotes (e.g. \"resource_name\": \"My Resource\")",
				" - Other types use Godot syntax, e.g. `0.5`, `true`, `Vector2(1, 2)`, `Color(1, 0, 0, 1)`",
				" - To reference a saved resource: `Resource(\"res://path/to/file.tres\")`",
				" - To create an embedded resource: `Object(ClassName, \"prop\": value, ...)`, e.g. `Object(SphereMesh,\"radius\":2.0)`. Do NOT use SubResource() or ExtResource() - those only work inside .tscn files.",
				"If any property name or value is invalid, an error is returned and the resource is NOT created."
			],
			"inputSchema": {
				"type": "object",
				"properties": {
					"file_path": {
						"type": "string",
						"description": "Path to the .tres file to create, relative to the project root"
					},
					"resource_type": {
						"type": "string",
						"description": "The resource type"
					},
					"properties": {
						"type": "object",
						"description": "Keys are the Godot property names to set",
						"additionalProperties": {
							"type": "string",
							"description": "The property value as a string, in Godot variant syntax (see the tool description)"
						}
					}
				}
			},
			"outputSchema": {
				"type": "object",
				"properties": {
					"success": {
						"type": "boolean",
						"description": "True if the resource was created and saved"
					}
				},
				"required": ["success"]
			}
		},
		"open_resource": {
			"title": "Open Resource",
			"description": "Opens a resource in the inspector.",
			"inputSchema": {
				"type": "object",
				"properties": {
					"file_path": {
						"type": "string",
						"description": "Path to the .tres file to open, relative to the project root"
					}
				}
			},
			"outputSchema": {
				"type": "object",
				"properties": {
					"success": {
						"type": "boolean",
						"description": "True if the resource was opened in the inspector"
					}
				},
				"required": ["success"]
			}
		},
		"get_resource_properties": {
			"title": "Get Resource Properties",
			"description": [
				"Gets the values of properties on a saved resource file (e.g. a .tres file).",
				"",
				"If 'properties' is omitted, returns an object that maps all of the resource's property names to values.",
				"If 'properties' is given, returns an object with a key for each requested property.",
				"Property values are strings in Godot variant syntax:",
				" - String properties are returned raw, without quotes",
				" - Other types use Godot syntax, e.g. `0.5`, `true`, `Vector2(1, 2)`, `Color(1, 0, 0, 1)`",
				" - A property referencing another saved resource is returned as `Resource(\"res://path/to/file.tres\")`",
				" - A property holding an embedded resource is summarized as `Object(ClassName)`, e.g. `Object(FastNoiseLite)`",
				"Requested properties may use colon-separated sub-paths to address properties nested inside embedded resources or other properties, e.g. `noise:frequency` or `albedo_color:r`.",
				"If a requested property holds an embedded resource (e.g. `noise`), all of that resource's properties are returned for it, instead of just a summary.",
				"If a requested property can't be resolved, its value will be an object with an `error` key."
			],
			"inputSchema": {
				"type": "object",
				"properties": {
					"file_path": {
						"type": "string",
						"description": "Path to the resource file, relative to the project root"
					},
					"properties": {
						"type": "array",
						"items": {
							"type": "string",
							"description": "The name of a property to get (colon-separated sub-paths allowed, e.g. `albedo_color:r`)"
						},
						"description": "The properties to get. If omitted, all of the resource's properties are returned."
					},
					"modified_only": {
						"type": "boolean",
						"description": "If true, only return properties that differ from their default value. Recommended when getting all properties, to keep the result small."
					}
				}
			},
			"outputSchema": {
				"type": "object",
				"description": "If 'properties' was omitted, a map of all the resource's property names to values. If 'properties' was given, a map of each requested property path to its result.",
				"additionalProperties": {
					"oneOf": [
						{
							"type": "object",
							"description": "A map of property names to values (in Godot variant syntax) - used both for the whole resource and for a requested property that holds an embedded resource - or an object with an 'error' key if the property path couldn't be resolved.",
							"additionalProperties": {
								"type": "string"
							}
						},
						{
							"type": "string",
							"description": "A single property value, in Godot variant syntax"
						}
					]
				}
			}
		},
		"set_resource_properties": {
			"title": "Set Resource Properties",
			"description": [
				"Sets the values of properties on a saved resource file (.tres or .res), and saves the changes to the file.",
				"",
				"Only pass in the properties you actually wish to change.",
				"EXAMPLE: {\"action\": \"Change color\", \"file_path\": \"res://thing.tres\", \"properties\": {\"albedo_color\": \"Color(1, 0, 0, 1)\"}}",
				"Property values are strings in Godot variant syntax:",
				" - String properties take the raw string, with NO extra quotes (e.g. \"resource_name\": \"My Resource\")",
				" - Other types use Godot syntax, e.g. `0.5`, `true`, `Vector2(1, 2)`, `Color(1, 0, 0, 1)`",
				" - To reference another saved resource: `Resource(\"res://path/to/file.tres\")`",
				" - To create an embedded resource: `Object(ClassName, \"prop\": value, ...)`, e.g. `Object(FastNoiseLite,\"frequency\":0.1)`. Do NOT use SubResource() or ExtResource() - those only work inside .tscn files.",
				"Property names may use colon-separated sub-paths to set properties nested inside embedded resources or other properties, e.g. `noise:frequency`, `albedo_color:r`.",
				"Note: the changes are saved to the file immediately, and affect every scene using the resource.",
				"This uses the Godot editor's undo/redo system to set the properties, so as many set operations as possible should be done in a single call, so they can all be undone at once (undo also re-saves the file).",
				"If any property name or value is invalid, an error is returned and NO properties are changed.",
				"Note: Just because this returns successfully, doesn't mean all properties were able to be set to the requested value. Always check that the properties have the correct value afterwards."
			],
			"inputSchema": {
				"type": "object",
				"properties": {
					"action": {
						"type": "string",
						"description": "Human-readable description of the action that will be shown in Godot's undo/redo history"
					},
					"file_path": {
						"type": "string",
						"description": "Path to the .tres or .res file, relative to the project root"
					},
					"properties": {
						"type": "object",
						"description": "Keys are the Godot property names to set (colon-separated sub-paths allowed, e.g. `albedo_color:r`)",
						"additionalProperties": {
							"type": "string",
							"description": "The property value as a string, in Godot variant syntax (see the tool description)"
						}
					}
				}
			},
			"outputSchema": {
				"type": "object",
				"properties": {
					"success": {
						"type": "boolean",
						"description": "True if the properties were set and the file was saved. Note: this doesn't guarantee every property was set to the requested value."
					}
				},
				"required": ["success"]
			}
		},
		"get_selected_nodes": {
			"title": "Get Selected Nodes",
			"description": [
				"Gets the nodes currently selected in the editor for the current scene.",
				"",
				"Returns an object with a 'node_paths' key: an array of node paths (relative to the scene root) for the selected nodes.",
				"If no scene is open or nothing is selected, 'node_paths' is an empty array."
			],
			"outputSchema": {
				"type": "object",
				"properties": {
					"node_paths": {
						"type": "array",
						"description": "Paths of the selected nodes, relative to the scene root",
						"items": {
							"type": "string"
						}
					}
				},
				"required": ["node_paths"]
			}
		},
		"instantiate_scene": {
			"title": "Instantiate Scene",
			"description": [
				"Instantiates a saved scene (.tscn/.scn) as a child node in the current scene, like adding an instanced scene in the editor.",
				"",
				"Returns true and the path of the new instance node.",
				"This uses the Godot editor's undo/redo system."
			],
			"inputSchema": {
				"type": "object",
				"properties": {
					"parent_path": {
						"type": "string",
						"description": "Path to the parent node, relative to the scene root"
					},
					"scene_path": {
						"type": "string",
						"description": "Path to the scene file to instantiate, relative to the project root"
					},
					"name": {
						"type": "string",
						"description": "Optional name for the new instance node"
					}
				}
			},
			"outputSchema": {
				"type": "object",
				"properties": {
					"success": {
						"type": "boolean",
						"description": "True if the scene was instantiated"
					},
					"node_path": {
						"type": "string",
						"description": "The path of the new instance node, relative to the scene root"
					}
				},
				"required": ["success", "node_path"]
			}
		},
		"save_scene": {
			"title": "Save Scene",
			"description": "Saves the scene currently open in the editor to its file. Fails if no scene is open, or if the current scene has never been saved (has no file path).",
			"outputSchema": {
				"type": "object",
				"properties": {
					"success": {
						"type": "boolean",
						"description": "True if the scene was saved"
					},
					"scene_path": {
						"type": "string",
						"description": "The path the scene was saved to"
					}
				},
				"required": ["success", "scene_path"]
			}
		},
		"add_to_group": {
			"title": "Add Node to Groups",
			"description": [
				"Adds a node in the current scene to one or more groups, persistently (so they're saved with the scene).",
				"",
				"This uses the Godot editor's undo/redo system."
			],
			"inputSchema": {
				"type": "object",
				"properties": {
					"node_path": {
						"type": "string",
						"description": "Path to the node, relative to the scene root"
					},
					"groups": {
						"type": "array",
						"items": {
							"type": "string",
							"description": "A group name"
						},
						"description": "The groups to add the node to"
					}
				}
			},
			"outputSchema": {
				"type": "object",
				"properties": {
					"success": {
						"type": "boolean",
						"description": "True if the node was added to the groups"
					}
				},
				"required": ["success"]
			}
		},
		"remove_from_group": {
			"title": "Remove Node from Groups",
			"description": [
				"Removes a node in the current scene from one or more groups.",
				"",
				"This uses the Godot editor's undo/redo system."
			],
			"inputSchema": {
				"type": "object",
				"properties": {
					"node_path": {
						"type": "string",
						"description": "Path to the node, relative to the scene root"
					},
					"groups": {
						"type": "array",
						"items": {
							"type": "string",
							"description": "A group name"
						},
						"description": "The groups to remove the node from"
					}
				}
			},
			"outputSchema": {
				"type": "object",
				"properties": {
					"success": {
						"type": "boolean",
						"description": "True if the node was removed from the groups"
					}
				},
				"required": ["success"]
			}
		},
		"get_node_groups": {
			"title": "Get Node Groups",
			"description": [
				"Gets the groups that nodes in the current scene belong to.",
				"",
				"Returns an object with a key for each requested node path, containing an array of group names. Internal engine groups are omitted. If a node isn't found, its value is an empty array."
			],
			"inputSchema": {
				"type": "object",
				"properties": {
					"node_paths": {
						"type": "array",
						"items": {
							"type": "string",
							"description": "Path to a node, relative to the scene root"
						}
					}
				}
			},
			"outputSchema": {
				"type": "object",
				"description": "Maps each requested node path to an array of its group names (internal engine groups omitted). An empty array if the node wasn't found. Empty object if no scene is open.",
				"additionalProperties": {
					"type": "array",
					"items": {
						"type": "string"
					}
				}
			}
		},
		"connect_signal": {
			"title": "Connect Signal",
			"description": [
				"Connects a signal on one node to a method on another node, in the current scene. The connection is persistent (saved with the scene).",
				"",
				"Both nodes must be in the current scene. The signal must exist on the 'from' node, and the method on the 'to' node.",
				"This uses the Godot editor's undo/redo system."
			],
			"inputSchema": {
				"type": "object",
				"properties": {
					"from_node": {
						"type": "string",
						"description": "Path to the node emitting the signal, relative to the scene root"
					},
					"signal": {
						"type": "string",
						"description": "Name of the signal to connect"
					},
					"to_node": {
						"type": "string",
						"description": "Path to the node receiving the signal, relative to the scene root"
					},
					"method": {
						"type": "string",
						"description": "Name of the method to call on the receiving node"
					}
				}
			},
			"outputSchema": {
				"type": "object",
				"properties": {
					"success": {
						"type": "boolean",
						"description": "True if the signal was connected"
					}
				},
				"required": ["success"]
			}
		},
		"disconnect_signal": {
			"title": "Disconnect Signal",
			"description": [
				"Disconnects a previously-connected signal between two nodes in the current scene.",
				"",
				"This uses the Godot editor's undo/redo system."
			],
			"inputSchema": {
				"type": "object",
				"properties": {
					"from_node": {
						"type": "string",
						"description": "Path to the node emitting the signal, relative to the scene root"
					},
					"signal": {
						"type": "string",
						"description": "Name of the signal to disconnect"
					},
					"to_node": {
						"type": "string",
						"description": "Path to the node receiving the signal, relative to the scene root"
					},
					"method": {
						"type": "string",
						"description": "Name of the connected method on the receiving node"
					}
				}
			},
			"outputSchema": {
				"type": "object",
				"properties": {
					"success": {
						"type": "boolean",
						"description": "True if the signal was disconnected"
					}
				},
				"required": ["success"]
			}
		},
		"attach_script": {
			"title": "Attach Script",
			"description": [
				"Attaches an existing script file to a node in the current scene.",
				"",
				"The script's base type must be compatible with the node's type. Use create_script first if the script doesn't exist yet.",
				"This uses the Godot editor's undo/redo system."
			],
			"inputSchema": {
				"type": "object",
				"properties": {
					"node_path": {
						"type": "string",
						"description": "Path to the node, relative to the scene root"
					},
					"script_path": {
						"type": "string",
						"description": "Path to the script file, relative to the project root"
					}
				}
			},
			"outputSchema": {
				"type": "object",
				"properties": {
					"success": {
						"type": "boolean",
						"description": "True if the script was attached"
					}
				},
				"required": ["success"]
			}
		},
		"detach_script": {
			"title": "Detach Script",
			"description": "Removes the script attached to a node in the current scene. This uses the Godot editor's undo/redo system.",
			"inputSchema": {
				"type": "object",
				"properties": {
					"node_path": {
						"type": "string",
						"description": "Path to the node, relative to the scene root"
					}
				}
			},
			"outputSchema": {
				"type": "object",
				"properties": {
					"success": {
						"type": "boolean",
						"description": "True if the script was detached"
					}
				},
				"required": ["success"]
			}
		},
		"create_script": {
			"title": "Create Script",
			"description": [
				"Creates a new GDScript file. Does NOT open or attach it.",
				"",
				"If 'content' is given, it is used as the file's contents. Otherwise, a minimal script extending 'base_class' is generated."
			],
			"inputSchema": {
				"type": "object",
				"properties": {
					"file_path": {
						"type": "string",
						"description": "Path to the .gd file to create, relative to the project root"
					},
					"base_class": {
						"type": "string",
						"description": "Base class for the generated script when 'content' is omitted (default: Node)"
					},
					"content": {
						"type": "string",
						"description": "Full contents of the script file. If omitted, a minimal template is generated."
					}
				}
			},
			"outputSchema": {
				"type": "object",
				"properties": {
					"success": {
						"type": "boolean",
						"description": "True if the script file was created"
					}
				},
				"required": ["success"]
			}
		},
		"open_script": {
			"title": "Open Script",
			"description": "Opens a script file in the editor's script editor.",
			"inputSchema": {
				"type": "object",
				"properties": {
					"file_path": {
						"type": "string",
						"description": "Path to the .gd file to open, relative to the project root"
					}
				}
			},
			"outputSchema": {
				"type": "object",
				"properties": {
					"success": {
						"type": "boolean",
						"description": "True if the script was opened in the script editor"
					}
				},
				"required": ["success"]
			}
		},
		"save_script": {
			"title": "Save Script",
			"description": [
				"Saves a script's unsaved changes from the editor to its file.",
				"",
				"If the script is open in the editor, its current (possibly unsaved) contents are written to disk. If it isn't open, there is nothing to save and this is a no-op."
			],
			"inputSchema": {
				"type": "object",
				"properties": {
					"file_path": {
						"type": "string",
						"description": "Path to the .gd file, relative to the project root"
					}
				}
			},
			"outputSchema": {
				"type": "object",
				"properties": {
					"success": {
						"type": "boolean",
						"description": "True if the operation succeeded"
					},
					"saved": {
						"type": "boolean",
						"description": "True if the script was open and its contents were written to disk; false if it wasn't open (nothing to save)"
					}
				},
				"required": ["success", "saved"]
			}
		},
		"read_script": {
			"title": "Read Script",
			"description": [
				"Reads the contents of a script file.",
				"",
				"If the script is currently open in the editor, its live (possibly unsaved) buffer is returned and 'open_in_editor' is true. Otherwise the contents are read from disk.",
				"Returns 'content' and 'open_in_editor'."
			],
			"inputSchema": {
				"type": "object",
				"properties": {
					"file_path": {
						"type": "string",
						"description": "Path to the .gd file, relative to the project root"
					}
				}
			},
			"outputSchema": {
				"type": "object",
				"properties": {
					"content": {
						"type": "string",
						"description": "The contents of the script (the live editor buffer if open, otherwise the file on disk)"
					},
					"open_in_editor": {
						"type": "boolean",
						"description": "True if the script is currently open in the editor (so 'content' is the live buffer)"
					}
				},
				"required": ["content", "open_in_editor"]
			}
		},
		"write_script": {
			"title": "Write Script",
			"description": [
				"Replaces the entire contents of an existing script file.",
				"",
				"You must read the script with read_script first: this fails if the script has changed since you last read it (or created/wrote it), so you don't overwrite edits you haven't seen. If it fails for that reason, read the script again and reapply your change.",
				"If the script is open in the editor, its editor buffer is updated instead of the file on disk (so unsaved edits aren't silently clobbered); 'saved' will be false, and you can call save_script to write it to disk. If it isn't open, the file is written directly and 'saved' is true.",
				"Use create_script to make a new script."
			],
			"inputSchema": {
				"type": "object",
				"properties": {
					"file_path": {
						"type": "string",
						"description": "Path to the .gd file, relative to the project root"
					},
					"content": {
						"type": "string",
						"description": "The full new contents of the script"
					}
				}
			},
			"outputSchema": {
				"type": "object",
				"properties": {
					"success": {
						"type": "boolean",
						"description": "True if the script was written"
					},
					"open_in_editor": {
						"type": "boolean",
						"description": "True if the script was open, so its editor buffer was updated rather than the file on disk"
					},
					"saved": {
						"type": "boolean",
						"description": "True if the new contents were written to disk; false if only the editor buffer was updated (call save_script to flush it)"
					}
				},
				"required": ["success", "open_in_editor", "saved"]
			}
		},
		"run_project": {
			"title": "Run Project",
			"description": [
				"Runs the project from the editor.",
				"",
				"'scene' controls what runs: omit it or use 'main' to run the project's main scene, 'current' to run the scene open in the editor, or a path to run a specific scene file."
			],
			"inputSchema": {
				"type": "object",
				"properties": {
					"scene": {
						"type": "string",
						"description": "What to run: 'main' (default), 'current', or a path to a scene file"
					}
				}
			},
			"outputSchema": {
				"type": "object",
				"properties": {
					"success": {
						"type": "boolean",
						"description": "True if the project started running"
					}
				},
				"required": ["success"]
			}
		},
		"stop_project": {
			"title": "Stop Project",
			"description": "Stops the running project, if any.",
			"outputSchema": {
				"type": "object",
				"properties": {
					"success": {
						"type": "boolean",
						"description": "True (the running project, if any, was stopped)"
					}
				},
				"required": ["success"]
			}
		},
		"reimport": {
			"title": "Reimport Assets",
			"description": [
				"Reimports one or more asset files (e.g. after changing the source file or its import settings).",
				"",
				"Each path must be an imported asset (i.e. have a .import file). If any path is invalid, nothing is reimported."
			],
			"inputSchema": {
				"type": "object",
				"properties": {
					"file_paths": {
						"type": "array",
						"items": {
							"type": "string",
							"description": "Path to an imported asset, relative to the project root"
						}
					}
				}
			},
			"outputSchema": {
				"type": "object",
				"properties": {
					"success": {
						"type": "boolean",
						"description": "True if the assets were reimported"
					}
				},
				"required": ["success"]
			}
		},
		"get_import_settings": {
			"title": "Get Import Settings",
			"description": [
				"Gets the import settings for an imported asset (e.g. a texture or audio file).",
				"",
				"Returns 'importer' (the name of the importer used) and 'options' (a map of import option names to values, in Godot variant syntax)."
			],
			"inputSchema": {
				"type": "object",
				"properties": {
					"file_path": {
						"type": "string",
						"description": "Path to the imported asset, relative to the project root"
					}
				}
			},
			"outputSchema": {
				"type": "object",
				"properties": {
					"importer": {
						"type": "string",
						"description": "The name of the importer used for this asset"
					},
					"options": {
						"type": "object",
						"description": "A map of import option names to values, in Godot variant syntax",
						"additionalProperties": {
							"type": "string"
						}
					}
				},
				"required": ["importer", "options"]
			}
		},
		"set_import_settings": {
			"title": "Set Import Settings",
			"description": [
				"Changes the import settings for an imported asset, and reimports it.",
				"",
				"Only pass the options you wish to change. Option values are strings in Godot variant syntax (e.g. `2`, `true`, `2.0`); a value that isn't valid variant syntax is taken as a raw string.",
				"Use get_import_settings to see the available option names and the current importer."
			],
			"inputSchema": {
				"type": "object",
				"properties": {
					"file_path": {
						"type": "string",
						"description": "Path to the imported asset, relative to the project root"
					},
					"importer": {
						"type": "string",
						"description": "Optional: change the importer used for this asset"
					},
					"options": {
						"type": "object",
						"description": "Import options to set (keys are option names)",
						"additionalProperties": {
							"type": "string",
							"description": "The option value, as a string in Godot variant syntax"
						}
					}
				}
			},
			"outputSchema": {
				"type": "object",
				"properties": {
					"success": {
						"type": "boolean",
						"description": "True if the import settings were changed and the asset was reimported"
					}
				},
				"required": ["success"]
			}
		},
		"get_editor_settings": {
			"title": "Get Editor Settings",
			"description": [
				"Gets settings from the Godot editor itself (the values stored in the global editor configuration, e.g. 'interface/editor/code_font_size' or 'text_editor/appearance/whitespace/draw_tabs'). These are editor-wide preferences, not project settings.",
				"",
				"Pass 'names' to get specific settings by name (returned regardless of whether they match their default).",
				"Otherwise, all settings are returned, but only those whose value differs from the default; pass 'include_defaults' as true to also include settings left at their default value.",
				"",
				"Returns 'settings': a map of setting name to value, in Godot variant syntax."
			],
			"inputSchema": {
				"type": "object",
				"properties": {
					"names": {
						"type": "array",
						"description": "Optional: specific setting names to get. If omitted, all settings are returned.",
						"items": {
							"type": "string"
						}
					},
					"include_defaults": {
						"type": "boolean",
						"description": "When getting all settings, also include those left at their default value (default: false)"
					}
				}
			},
			"outputSchema": {
				"type": "object",
				"properties": {
					"settings": {
						"type": "object",
						"description": "A map of setting name to value, in Godot variant syntax",
						"additionalProperties": {
							"type": "string"
						}
					}
				},
				"required": ["settings"]
			}
		},
		"set_editor_settings": {
			"title": "Set Editor Settings",
			"description": [
				"Changes settings in the Godot editor itself (editor-wide preferences, not project settings). Changes take effect immediately and are persisted by the editor.",
				"",
				"Pass 'settings': a map of setting name (e.g. 'interface/editor/code_font_size') to value. Values are strings in Godot variant syntax (e.g. `16`, `true`, `2.0`); a value that isn't valid variant syntax is taken as a raw string.",
				"Use get_editor_settings to see the available setting names and their current values."
			],
			"inputSchema": {
				"type": "object",
				"properties": {
					"settings": {
						"type": "object",
						"description": "Settings to set (keys are setting names)",
						"additionalProperties": {
							"type": "string",
							"description": "The setting value, as a string in Godot variant syntax"
						}
					}
				},
				"required": ["settings"]
			},
			"outputSchema": {
				"type": "object",
				"properties": {
					"success": {
						"type": "boolean",
						"description": "True if the settings were changed"
					}
				},
				"required": ["success"]
			}
		},
		"get_log_messages": {
			"title": "Get Log Messages",
			"description": [
				"Gets recent log output from the editor (print() output, warnings, and errors) captured since the editor started.",
				"",
				"Returns a 'messages' array of the most recent log lines (oldest first).",
				"Note: Only includes messages from code run in the editor, not from running the project."
			],
			"inputSchema": {
				"type": "object",
				"properties": {
					"count": {
						"type": "integer",
						"description": "Maximum number of recent messages to return (default 100). Use 0 for all retained messages."
					}
				}
			},
			"outputSchema": {
				"type": "object",
				"properties": {
					"messages": {
						"type": "array",
						"description": "The most recent log lines, oldest first",
						"items": {
							"type": "string"
						}
					}
				},
				"required": ["messages"]
			}
		},
		"execute_editor_script": {
			"title": "Execute Editor Script",
			"description": [
				"Executes the given GDScript code in the editor, in the context of a Node that is a child of the scene currently being edited.",
				"",
				"For modifying the current scene, use the other tools when possible.",
				"However, if you do use a script to modify the current scene, you MUST use `EditorUndoRedoManager` from `EditorInterface.get_editor_undo_redo()`, and the action name MUST end with \"(AI)\".",
				"You can find nodes relative to the scene root using `EditorInterface.get_edited_scene_root().get_node_or_null(node_path)`.",
				"Two helper methods have been provided:",
				" - `func editor_undo_redo_create_node(p_undo_redo: EditorUndoRedoManager, p_parent: Node, p_child: Node) -> void`",
				" - `func editor_undo_redo_remove_node(p_undo_redo: EditorUndoRedoManager, p_parent: Node, p_child: Node) -> void`",
				"If you are using `EditorUndoRedoManager` to add or remove a node, you MUST call one of those helper methods before calling `commit_action()`. This will add some `add_do_method()` and `add_undo_method()` calls to ensure the changes are synchronized to the live game if the game is running.",
				"If the script successfully runs, the output from `print()` will be returned."
			],
			"inputSchema": {
				"type": "object",
				"properties": {
					"code": {
						"type": "string",
						"description": "A snippet of GDScript code. It should use tabs rather than spaces for indentation. Don't wrap the code in a `_ready()` function."
					}
				}
			},
			"outputSchema": {
				"type": "object",
				"properties": {
					"success": {
						"type": "boolean",
						"description": "True if the script ran successfully"
					},
					"output": {
						"type": "array",
						"description": "The lines printed by the script via print()",
						"items": {
							"type": "string"
						}
					}
				},
				"required": ["success", "output"]
			}
		},
		"restart_editor": {
			"title": "Restart Editor",
			"description": [
				"Restarts the Godot editor for the project.",
				"",
				"The editor first prompts the user to save their work, and only restarts once they confirm.",
				"",
				"By default, unsaved changes are saved before restarting; pass 'skip_save' as true to discard them instead.",
				"If the user declines the prompt, the editor is not restarted and this returns an error."
			],
			"inputSchema": {
				"type": "object",
				"properties": {
					"skip_save": {
						"type": "boolean",
						"description": "Discard unsaved changes instead of saving them before restarting (default: false)"
					}
				}
			},
			"outputSchema": {
				"type": "object",
				"properties": {
					"success": {
						"type": "boolean",
						"description": "True once the editor has restarted and reconnected"
					}
				},
				"required": ["success"]
			}
		},
		"close_editor": {
			"title": "Close Editor",
			"description": [
				"Closes the Godot editor for the project.",
				"",
				"The editor first prompts the user to save their work, and only closes once they confirm.",
				"",
				"By default, unsaved changes are saved before closing; pass 'skip_save' as true to discard them instead.",
				"If the user declines the prompt, the editor is not closed and this returns an error."
			],
			"inputSchema": {
				"type": "object",
				"properties": {
					"skip_save": {
						"type": "boolean",
						"description": "Discard unsaved changes instead of saving them before closing (default: false)"
					}
				}
			},
			"outputSchema": {
				"type": "object",
				"properties": {
					"success": {
						"type": "boolean",
						"description": "True once the user has accepted and the editor is closing"
					}
				},
				"required": ["success"]
			}
		}
	}
}
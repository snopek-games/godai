The main scene of this project (`res://main.tscn`) is empty apart from its
Node2D root.

Add a Sprite2D called `Player` at position (100, 50), and give it a script at
`res://player.gd` that moves it to the right at 200 pixels per second. Save the
scene when you're done.

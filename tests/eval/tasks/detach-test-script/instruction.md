The Camera2D in `res://main.tscn` still has `res://camera_shake_test.gd`
attached from an experiment. The node shouldn't have a script at all, but keep
the file around. Save the scene when you're done.

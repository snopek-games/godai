TODO
====

For a 1.0
---------

- Addon: Ability to configure "effort" level
- Addon: Make user_chat.tscn and error_chat.tscn expand for their text, but wrap when they right the max width
  - I think we can do this my measuring the length of each line, and if one is over a certain threshold, then we expand to fill the whole parent
- Addon: Have separate tools for MCP and API mode
  - API mode needs to have file management tools
    - See https://platform.claude.com/docs/en/agents-and-tools/tool-use/text-editor-tool
    - The tricky thing is that this is Anthropic specific (OpenAI has their own thing)
    - How does this overlap with our tools for script editing?
  - Having a way to "reload" the tools would help iteration during development
- Addon: allow enabling/disabling MCP or API mode entirely
  - MCP should have an "automatically start MCP server" option which acts like its enable/disable
  - API mode should have an "enabled" setting
- Addon: Implement support for more LLM APIs
  - Make 2-3 client backends:
    - `Anthropic`: "Anthropic"
    - `OpenAIChatCompletions`: "OpenAI Chat Completions - works everywhere"
    - `OpenAIResponses` (optional): "OpenAI Responses - OpenAI's newer agentic API"
  - The configuration then is:
    - API type (the backend from above)
    - API URL
    - API key
    - Model
  - Set of "profiles" which would be like:
    - OpenAI: `OpenAIResponses`, with real OpenAI URL
    - Anthropic (Claude): `Anthropic` with real Anthropic URL
    - Google (Gemini): `OpenAIChatCompletions` with Gemini URI
  - "Config" button in the "AI" panel
    - It should allow selecting one of the profiles or "custom", which gives you the underlying fields
    - This should basically reflect what we have in Editor Settings but friendlier
  - "Model" selection field in the "AI" panel
    - It'll need to query the list of available models
    - We might need profiles to have a "filter" for the models to include/exclude (and maybe an editor setting too?)
    - Or, we may want to pull the model list from the models.dev API? It allows us to filter based on capability
      - See https://models.dev/api.json
- Addon: Add a way to cancel a request
- Addon: Add system prompt
- Better installation story
  - Options:
  - Provide `brew` package (as a "cask", I guess?)
    - Provide `WinGet` package
    - Provide `curl | bash` style script for MacOS, Linux and Windows (it would be `irm | WinGet` for Windows)
  - Provide self-update feature
    - See https://github.com/creativeprojects/go-selfupdate
- Test thoroughly on Windows and MacOS!
- Add to Godot asset store
- Add to MCP Registry
  - https://registry.modelcontextprotocol.io/
  - See: https://modelcontextprotocol.info/tools/registry/publishing/
- Add MCPB to Claude directory
  - https://docs.google.com/forms/d/e/1FAIpQLScHtjkiCNjpqnWtFLIQStChXlvVcvX8NPXkMfjtYPDPymgang/viewform

Backlog
-------

- MCP: Add the ability to install Godot for the users platform
  - This could/should include some minor version management, keeping multiple Godot versions and using the right one for the project
  - Do we need a tool to download the export templates?
- Make a Godot plugin in the Claude marketplace (which includes the .mcp.json)
  - Users do `/plugin marketplace add snopek-games/godai-mcp` or `claude plugin install ...`
  - See: https://claude.com/blog/claude-code-plugins
  - See: https://github.com/alexmeckes/godot-claude-skills
- Addon: Add support for similar tools to everything in ee0pdt/Godot-MCP
- Addon: Chat history
  - Save and restore current chat when restarting the editor
  - Save a chat history of up to X conversations
- Addon: Convert markdown to bbcode?
  - See https://github.com/daenvil/MarkdownLabel
- Tool: Get Godot version and executeable path
- Tool: Get class documentation
  - Depends on https://github.com/godotengine/godot/pull/112628
- Tool: Capture log messages from the running game
  - get_log_messages only sees editor output; the game is a separate process
  - Inject a logger autoload that forwards lines via EngineDebugger.send_message
  - Receive them in the editor with an EditorDebuggerPlugin custom capture
- Addon: Allow editor plugins to register custom tools for a particular project
  - Will need to allow MCP to update it's tools list
- MCP: Add a command for creating a new project

TODO
====

0.5: Finish critical addon stuff
--------------------------------

- Addon: The prompt bar with the "New/Clear" button should still be shown on CLI/MCP chats when MCP/CLI isn't currently connected
- Addon: Showing the Godai dock should scroll to the bottom
- Addon: Add to Godot asset store

0.6: Evaluate, benchmark and improve effectiveness
--------------------------------------------------

- Addon: Add in game tools
  - Sending input
  - Taking a screenshot

Backlog
-------

- Add MCPB to Claude Desktop directory
  - https://docs.google.com/forms/d/e/1FAIpQLScHtjkiCNjpqnWtFLIQStChXlvVcvX8NPXkMfjtYPDPymgang/viewform
- Eval: Look at existing gamedev benchmarking suites and see if we can use them with Godai
  - Can we run those test suites with Godai added to the mix?
  - Can we adapt their tasks and bring them into our eval framework?
  - Links:
    - See https://github.com/waynchi/gamedevbench
    - See https://github.com/FreedomIntelligence/gamecraft-bench
- CLI: Add command to create a new project
    - Sketch:
      ```
      # Will only work on non-existent directories, or empty directories. If no directory is given, use the CWD
      godai project new --name="Blah" --godot-version=4.5 path/to/use
      # Or is `create` better than new `new`?
      ```
    - Allow creating from templates!
- MCP: Add a way to search project and editor settings (maybe a variation on get_project/editor_settings which does a string contains?)
- CLI: How to make the descriptions for `editor-tool` more CLI-friendly?
- Make a Godot plugin in the Claude marketplace (which includes the .mcp.json)
  - Users do `/plugin marketplace add snopek-games/godai` or `claude plugin install ...`
  - See: https://claude.com/blog/claude-code-plugins
  - See: https://github.com/alexmeckes/godot-claude-skills
- Addon: Have separate tools for MCP and API mode
  - API mode needs to have file management tools
    - See https://platform.claude.com/docs/en/agents-and-tools/tool-use/text-editor-tool
    - The tricky thing is that this is Anthropic specific (OpenAI has their own thing)
    - How does this overlap with our tools for script editing?
  - Having a way to "reload" the tools would help iteration during development
- Addon: Session usability
  - Allow manually renaming sessions
  - Allow manually deleting sessions
- Addon: Use LLM to automatically set titles for sessions
- Addon: Record errors are part of chat session? (but don't send them to the AI)
- Addon: Add support for similar tools to everything in ee0pdt/Godot-MCP
- Addon: Add support for block quotes (and other recursive blocks?) to Markdown to BBCode converter
- Tool: Get Godot version and executeable path
- Tool: Get class documentation
  - Depends on https://github.com/godotengine/godot/pull/112628
- Addon: Allow editor plugins to register custom tools for a particular project
  - Will need to allow MCP to update it's tools list
- Addon: Make user_chat.tscn and error_chat.tscn expand for their text, but wrap when they right the max width
  - I think we can do this my measuring the length of each line, and if one is over a certain threshold, then we expand to fill the whole parent
